# Lessons

> Generated by `tools/workforce` at 2026-09-25T05:53:43.485Z (policy sha256:91e307642fb1…). Derived view — do not hand-edit.

- Track implementation identity by git tree hash, not commit SHA, while work is local-only in a wipe-prone sandbox. (JRN-0005)
- Engine gap: Manager-executed governance tasks (TASK-0001..0003) cannot reach COMPLETED because TASK_COMPLETE requires a worker assignment, lease, MQ item and evidence chain. Not faked by assigning to a worker. Proposed follow-up: a Manager-executed task completion path (small PR, GOVERNANCE). (JRN-0011)
- Browser gate settings-compat "usage page loaded GET /rest/license" waits a fixed 2.5s after navigation; on PR #283 it saw no request (job 107955793108) and passed on rerun of the same head (job 107958710236). Classified flaky timing (measured). Rerun the single job on the same head before classifying; candidate hardening: wait for the request instead of a fixed sleep. (JRN-0019)
- Re-recorded JRN-0014: /tmp is RAM-backed (2 GB); leftover clones push lego-scale-stress into SIGKILL. Environmental, not a regression; ship.sh clears /tmp before verification. (JRN-0022)
- Re-recorded JRN-0015: test both branches of CI shell logic locally under the runner shell flags (bash -e -o pipefail) before shipping (regression #278, hotfix #280). (JRN-0023)
- Re-recorded JRN-0017: run workforce tests as node --test tools/workforce/test/*.test.mjs; passing the directory also runs fixtures/concurrent-worker.mjs and reports a false failure. (JRN-0024)
- The workspace snapshot is best-effort (file cap about 10,000); full repo clones in the home dir push the store past it. Keep clones out of the persisted workspace at turn end and push the raw store to arena-manager (manager-memory/state) after every mutation. (JRN-0025)
