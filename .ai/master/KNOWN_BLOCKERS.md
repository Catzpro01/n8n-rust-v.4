<!-- GENERATED by tools/lego/ai-pack.mjs — do not edit by hand. Run `npm run lego:ai` after changing the manifest. -->
# Known blockers

**Canonical for:** what is currently preventing progress, and who owns it.

> A blocker is closed by evidence, never by a report deadline. Downgrading a blocker to make a status document look complete is the specific failure this register exists to prevent.

> **Plane.** A `product` blocker constrains the n8n LEGO product itself. An
> `engineering-operations` blocker constrains how this repository is *built* —
> the development workforce, its control planes and its arbitration. The latter
> is not product architecture and never becomes a product requirement, a LEGO
> domain or a runtime dependency; it is recorded here only so that it is owned
> and visible. See `docs/engineering-operations/workforce-governance.json`.

## BL-1 — Execution id allocation is process-local

| | |
| --- | --- |
| Severity | **class-A** |
| Status | **open** |
| Plane | **product** |
| Owner | `agent-5` |
| Where | `apps/n8n-lego/src/store.mjs` |
| Scheduled | P8 |

**Consequence:** Two processes allocate the same execution id. Multi-process operation is unsafe.

**Resolution boundary:** storage contract -> durable identity allocation

## BL-2 — Local JSON is the system of record

| | |
| --- | --- |
| Severity | **class-A** |
| Status | **open** |
| Plane | **product** |
| Owner | `agent-5` |
| Where | `apps/n8n-lego/src/store.mjs` |
| Scheduled | P8 |

**Consequence:** No shared persistence, so no second instance can serve the same data.

**Resolution boundary:** storage contract -> shared persistence implementation (local -> SQLite -> Postgres or another shared backend), with no consumer change

## BL-4 — Cross-agent vocabulary arbitration is open

| | |
| --- | --- |
| Severity | **governance** |
| Status | **open** |
| Plane | **engineering-operations** |
| Owner | `manager` |
| Reference | docs/n8n-lego/decisions/cross-agent-decisions.backend.json |

**Consequence:** XA-8, XA-9, XA-10, XA-11, XA-12 and XA-13 remain unresolved. Agent 1 and Agent 2 are aligned today by convention plus publicationPending records, not by a decision.

## BL-5 — Legacy i18n ownership is unresolved

| | |
| --- | --- |
| Severity | **governance** |
| Status | **open** |
| Plane | **engineering-operations** |
| Owner | `manager` |
| Reference | XA-13 |

**Consequence:** Translation is now an official LEGO target, but the legacy i18n route is still served by the legacy aggregate and has no declared owner. It must not be forced into credentials, node-registry or workflow merely to satisfy legacy routing.

## BL-6 — External control planes are unverified from this repository

| | |
| --- | --- |
| Severity | **governance** |
| Status | **open** |
| Plane | **engineering-operations** |
| Owner | `manager` |

**Consequence:** The Supabase control plane and the VPS execution gate are declared in the project plan but have no schema, client, endpoint or credential in this tree. Their operational status cannot be asserted from here.

## BL-3 — The frontend browser gate cannot run in the dev sandbox

| | |
| --- | --- |
| Severity | **environmental** |
| Status | **open — CI-only, not a code defect** |
| Plane | **product** |
| Owner | `agent-1` |
| Where | `tests/e2e` |

**Consequence:** Puppeteer cannot download Chromium here, so the browser gate is CI-only. Local runs prove nothing about the editor UI.

