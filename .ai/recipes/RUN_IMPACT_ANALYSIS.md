<!-- GENERATED by tools/lego/ai-pack.mjs — do not edit by hand. Run `npm run lego:ai` after changing the manifest. -->
# RUN_IMPACT_ANALYSIS — Find out what a change would break

## Prerequisites

_none_

## Steps

1. `node tools/lego/impact-graph.mjs --target <id>` — who depends on it
2. `node tools/lego/impact-graph.mjs --plan --target <id> --change "<what>"` — the full plan
3. `node tools/lego/impact-graph.mjs --changed <files>` — which tests to run

## Required tests

- `whatever the selection returns`

## Merge gate

n/a — advisory

## Rollback condition

n/a
