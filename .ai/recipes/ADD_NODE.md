<!-- GENERATED by tools/lego/ai-pack.mjs — do not edit by hand. Run `npm run lego:ai` after changing the manifest. -->
# ADD_NODE — Add a node

## Prerequisites

- You ran the decision model
- The node identity is new and stable

## Steps

1. Run `decideCreationRoute(facts)` and record the rule that fired.
2. Write the Node Contract descriptor: identity, manifest, parameters, io, credentials, execution, events, errors, capabilities, portability, runtimeRequirements, resources, trust.
3. Declare the portability profile honestly — a node that needs a subprocess is not `portable`.
4. Declare the resource profile as metadata; there is no scheduler reading it yet.

## Required tests

- `npm run lego:foundation`
- `contract conformance for the descriptor`

## Merge gate

Descriptor complete and route recorded

## Rollback condition

Remove the descriptor; nothing else references it.
