id: TASK-412-workflow-isolation-gate
agent: orchestrator-workflow-owner   # = Agent 1 workflow lane
lego: workflow
module: packages/workflow-lego
phase: 2/3 seam (structural isolation verification)

# RESTORE THE WORKFLOW-LEGO ISOLATION GATE TO GREEN.
# Discovered during the 2026-09-17 dual-phase sweep: `node tools/workflow-isolation-gate.mjs`
# reported 6/11 with G06–G10 failing on "typescript missing" — a broken verification
# environment, not behaviour. The isolation gate is the workflow LEGO's own 11-gate check
# (boundary, kernel snapshot, port surface, reference hash, extraction, 2× tsc, tests,
# digest, strict mode, engine 'live' harness R0–R6) and is the one "11/11" label that the
# handoff says IS runnable from this sandbox (docs/isolation/workflow-handoff.md §7.1).

allowed_paths:
  - packages/workflow-lego/**        # deps install state only; source untouched
  - docs/isolation/evidence/**       # gate-regenerated artifacts
  - docs/isolation/workflow-verification.md
  - tasks/TASK-412-workflow-isolation-gate.yaml
  - results/TASK-412-workflow-isolation-gate.md

forbidden_paths:
  - reference/n8n/**
  - crates/**                         # nothing Rust-side is needed for this gate
  - packages/workflow-lego/src/**     # if a green gate required source edits, that would
                                     # be a behaviour change and belongs to a new task

acceptance_criteria:
  - node tools/workflow-isolation-gate.mjs → "gates: 11/11 PASS · BEHAVIOR CHANGE: NONE DETECTED"
  - G09 digest: 252 sections, 0 differences
  - evidence artifacts regenerated by the run and committed (they are run-output, not goldens)
  - label discipline (handoff §7.1): this is `isolation_gate_11/11`, NOT the VPS smoke 11/11
