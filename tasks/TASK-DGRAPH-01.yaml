id: TASK-DGRAPH-01
agent: arena/01a0aff8-n8n-rust-v-4
lego: workflow-model
module: start-node-navigation
phase: 3
title: "Executable D-01..D-04 golden (observed) + 1:1 getHighestNode/getStartNode reconstruction — ISSUE-015/017 evidence base"
status: SUBMITTED_FOR_REVIEW

# Context: the audit (TASK-AUDIT-ISSUES-01 / ISSUE-025 table) records that
# tests/reference/04-disabled-node/ and 05-cyclic-invalid/ contain workflow.json only —
# "the D-04 prediction currently has no executable golden on this branch". Agent 5's
# expected.json asset never landed. ISSUE-015 (corrected scope) pins the reference
# semantics: getHighestNode workflow.ts :498 (`disabled === false`, STRICT self-test)
# vs :553 (`disabled !== true`, LOOSE parent fallback) — the D-04 asymmetry;
# getStartNode :824 (`!node.disabled`), :839/:853 (`disabled === true → continue`).
# ISSUE-017 (HIGH, proven): the Rust port implements neither; the probe was re-run in
# TASK-RIG-REPAIR-01 and the divergence stands.
#
# This task lands the permanent asset the audit asks for:
#   1. tests/reference/04-disabled-node/case.json      — probe definitions (inputs)
#   2. tests/reference/harness/disabled-graph.js       — observer driving the REAL
#      n8n-workflow@2.9.1 (never hand-written snapshots; UPDATE=1 regenerates)
#   3. tests/reference/04-disabled-node/expected.json  — OBSERVED behaviour
#   4. packages/workflow-model-lego/src/start-node-navigation.ts — 1:1 JS/TS port
#   5. a consumer test: reconstruction vs expected.json (falsifiable)
# The Rust port owner (out of this task's lane) can now consume the same golden.
#
# Scope warning (ISSUE-015 correction): plain traversal must NOT assert disabled-filtering
# (graph-utils.ts has no disabled concept) — only these two methods are behavioural.

allowed_paths:
  - tasks/TASK-DGRAPH-01.yaml
  - tests/reference/04-disabled-node/**
  - tests/reference/harness/disabled-graph.js
  - packages/workflow-model-lego/**     # additive module + test (+ index export, build)
  - results/TASK-DGRAPH-01.md
  - docs/isolation/CROSS-AGENT-ISSUES.md

forbidden_paths:
  - crates/**
  - reference/n8n/**
  - packages/frontend/**
  - packages/workflow-lego/**           # boundary-gated kernel lane — untouched

requires:
  - docs/isolation/CROSS-AGENT-ISSUES.md (ISSUE-015 corrected scope, ISSUE-017, ISSUE-025 addendum)
  - packages/workflow-lego/node_modules (real n8n-workflow@2.9.1 for observation)

operations:
  - name: write_case_probes
  - name: observe_real_runtime
  - name: port_navigation_1to1
  - name: executable_consumer_test
  - name: falsifiability_control
  - name: full_matrix_and_record

acceptance:
  - expected.json generated by UPDATE=1 observation of n8n-workflow@2.9.1, never hand-written
  - reconstruction matches expected.json on every probe (deep equal)
  - falsifiability: normalising the asymmetry away makes the consumer fail
  - workflow-model-lego suite green · verify:all real exit 0 · no other lane regressed
