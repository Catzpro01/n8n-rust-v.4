id: TASK-405-phase3-connection-lego
agent: arena-worker
lego: connection
module: connection
phase: 3
track: "JavaScript/TypeScript reconstruction (docs/isolation/PHASE-3-OPENING-RECORD.md §2)"
title: "Reconstruct the Connection Model in TypeScript and accept it with the reference-recorded goldens"
status: SUBMITTED_FOR_REVIEW
authority: PROJECT_RULES.md §1, §3, §6 · contracts/connection.contract.md §7 · docs/isolation/PHASE-3-OPENING-RECORD.md

objective: >
  Phase 2 delivered the Connection boundary and contract but no implementation on the JS/TS track.
  Reconstruct the seven files the Connection LEGO owns, 1:1 against reference/n8n 2.9.4, in
  TypeScript under packages/, and prove equivalence against the acceptance set that was already
  recorded from the pinned reference runtime — never against hand-written expectations.

allowed_paths:
  - packages/connection-lego/**
  - packages/expression-lego/package.json      # ISSUE-022 runner fix only (no source change)
  - docs/isolation/LEGO-MASTER-MAP.md
  - docs/isolation/CROSS-AGENT-ISSUES.md
  - README.md
  - package.json
  - .gitignore
  - results/TASK-405-phase3-connection-lego.md
  - tasks/TASK-405-phase3-connection-lego.yaml

forbidden_paths:
  - reference/n8n/**
  - crates/**                                  # Rust port track — not this task's track
  - apps/**
  - packages/workflow-lego/**
  - packages/execution-engine/**
  - tests/reference/**/expected.json           # oracles are read-only
  - tests/reference/workflow-rust/fixtures.json

requires:
  - contracts/connection.contract.md
  - contracts/workflow.contract.md
  - docs/isolation/connection.md
  - tests/reference/workflow-rust/fixtures.json
  - tests/reference/connection/01-linear/expected.json

reconstruction:
  - { from: "reference/n8n/packages/workflow/src/common/get-node-by-name.ts", to: "packages/connection-lego/src/get-node-by-name.ts", lines: 19 }
  - { from: "reference/n8n/packages/workflow/src/common/get-connected-nodes.ts", to: "packages/connection-lego/src/get-connected-nodes.ts", lines: 98 }
  - { from: "reference/n8n/packages/workflow/src/common/get-child-nodes.ts", to: "packages/connection-lego/src/get-child-nodes.ts", lines: 12 }
  - { from: "reference/n8n/packages/workflow/src/common/get-parent-nodes.ts", to: "packages/connection-lego/src/get-parent-nodes.ts", lines: 18 }
  - { from: "reference/n8n/packages/workflow/src/common/map-connections-by-destination.ts", to: "packages/connection-lego/src/map-connections-by-destination.ts", lines: 49 }
  - { from: "reference/n8n/packages/workflow/src/graph/graph-utils.ts", to: "packages/connection-lego/src/graph-utils.ts", lines: 273 }
  - { from: "reference/n8n/packages/workflow/src/connections-diff.ts", to: "packages/connection-lego/src/connections-diff.ts", lines: 100 }

acceptance:
  - "tsc --strict builds with 0 errors"
  - "9/9 traversal fixture cases reproduce byte-for-byte"
  - "6/6 compareConnections fixture cases reproduce byte-for-byte"
  - "32/32 Connection-owned golden probes across tests/reference/connection/01..05 reproduce"
  - "the 14 wf.* probes are skipped by ownership, and the skip set is asserted"
  - "2 negative controls prove the oracle rejects plausible-but-wrong implementations"
  - "reference fixtures still match the pinned runtime (build-fixtures.mjs --check)"
  - "no regression anywhere on the branch"

verification:
  connection_lego: "52 tests / 52 pass / 0 fail"
  phase3_conformance: "42/42 CHECKS PASSED (Rust confined: 22 files, frozen surface 15/15)"
  boundary_audit: "AUDIT RESULT: PASS (all edges documented)"
  isolation_gate: "11/11 PASS · BEHAVIOR CHANGE: NONE DETECTED"
  execution_gate: "9/9 PASS"
  expression_lego: "46/46 PASS (after the ISSUE-022 runner fix)"
  reconstructed_engine: "28/28 PASS"
  reference_harness: "18 PASS / 0 FAIL / 0 UNKNOWN"
  fixtures_drift: "fixtures match the pinned reference: 8 checksum, 6 diff, 6 shape, 6 rename, 9 traversal cases"

notes:
  - "Reference quirks are reproduced, not fixed: unshift+splice traversal order, the no-op third expression in `for (i = addNodes.length; i--; i > 0)`, depth=-1 vs depth=0, sparse destination slots, JSON.stringify connection identity, identity-keyed adjacency Set, main-only root/leaf/path filtering, and the __proto__ node that never becomes an own key."
  - "D-08 (renameNode does not rebuild the destination index) stays an open Workflow-LEGO defect; getParentNodes here reads the destination map, which is what makes it observable."
  - "CORRECTION RECORDED: an earlier local attempt deleted crates/** on the strength of PROJECT_RULES §1 read against the stale base commit fc4e5631. That was wrong — Phase 3 is OPEN on this branch (docs/isolation/PHASE-3-OPENING-RECORD.md) and both harnesses accept Rust confined to crates/**+apps/**. The deletion was discarded before it reached the branch; nothing in crates/** or apps/** was changed by this task."
  - "Next on this track: Workflow LEGO — the 20 remaining fixture cases (checksum 8, toJSON 6, rename 6)."
