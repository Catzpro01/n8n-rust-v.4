id: TASK-CGRAPH-01
agent: arena/01a0aff8-n8n-rust-v-4
lego: integration
module: cyclic-graph-golden
phase: 3
title: "Executable golden for 05-cyclic-invalid (observed) — reference accepts the cycle; detection is the additive validator's job (R5/R6 oracle)"
status: VERIFIED

# Context: the audit table (TASK-AUDIT-ISSUES-01) records "04-disabled-node/ and
# 05-cyclic-invalid/ contain workflow.json only" and "no 05-cyclic disk read anywhere in
# crates/". TASK-DGRAPH-01 landed the 04 golden; this task lands 05.
#
# Ground truth (verified by grep this task): reference/n8n 2.9.4 has NO cycle validation —
# the string "cyclic" does not occur in packages/workflow/src or packages/core/src. The
# reference runtime CONSTRUCTS cyclic workflows happily; the only cycle-aware code on this
# branch is the ADDITIVE validation LEGO (tests/reference/agent-4/validation/workflow-rules.ts,
# ISSUE-003 Option A, NEW CAPABILITY — approved TS reference for the Rust port's
# detect_cycles, n8n-validation lib.rs:48).
#
# Therefore the golden records BOTH, clearly separated:
#   C-01  reference behaviour — real n8n-workflow@2.9.1 constructs the cycle;
#         Workflow.getStartNode() → undefined (a triggerless cycle has no start node)
#   C-02  reference behaviour — Connection LEGO traversal over the cycle is
#         cycle-safe (visited-set) and terminates: getChildNodes / getParentNodes
#   C-03  NEW CAPABILITY — validateWorkflow flags CYCLE_DETECTED with the exact
#         path/message (NOT reference behaviour; the scope warning says so)
# The Rust detect_cycles owner consumes C-03 as the R5 oracle; C-01/C-02 pin what must
# NOT be "fixed" (a port that rejects the cycle at construction would DIVERGE).

allowed_paths:
  - tasks/TASK-CGRAPH-01.yaml
  - tests/reference/05-cyclic-invalid/**
  - tests/reference/harness/cyclic-graph.js
  - packages/connection-lego/test/cyclic-golden.test.mjs   # additive consumer test
  - results/TASK-CGRAPH-01.md
  - docs/isolation/CROSS-AGENT-ISSUES.md

forbidden_paths:
  - crates/**
  - reference/n8n/**
  - packages/frontend/**
  - tests/reference/agent-4/**            # peer's validation LEGO — consumed read-only

requires:
  - tests/reference/05-cyclic-invalid/workflow.json (PHASE3-GATE-01 fixture)
  - tests/reference/agent-4/validation/workflow-rules.ts (additive validator)
  - packages/connection-lego (built dist), packages/workflow-lego/node_modules (real runtime)

operations:
  - name: write_case_probes
  - name: observe_real_runtime_and_additive_validator
  - name: consumer_test_with_falsifiability
  - name: full_matrix_and_record

acceptance:
  - expected.json generated by UPDATE=1 observation, never hand-written
  - consumer test deep-equals every probe; falsifier proves the golden detects real cycles
  - scope warning records that the reference runtime does NOT validate cycles
  - connection-lego suite green (additive) · verify:all real exit 0
