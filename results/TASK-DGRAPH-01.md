# TASK-DGRAPH-01 — Executable D-01..D-04 golden + 1:1 getHighestNode/getStartNode reconstruction

**Status: SUCCESS** — the audit's missing permanent asset now exists and is executable:
`tests/reference/04-disabled-node/expected.json` is an **OBSERVED** snapshot of
`n8n-workflow@2.9.1` (generated by `UPDATE=1 tests/reference/harness/disabled-graph.js`, never
hand-written), and `packages/workflow-model-lego/src/start-node-navigation.ts` reconstructs
`getHighestNode` / `__getStartNode` / `getStartNode` 1:1 from `workflow.ts` :495-562 + :817-883.
Lane suite **34/34** (26 + 8 new).

## Observed golden (5 probes, D-01..D-05)

| Probe | Observed (real runtime) | Reference |
| :--- | :--- | :--- |
| D-01 mid-chain disabled | highest("Set Output"/"Skipped Step") = `["Manual Trigger"]` | :495-562 |
| D-02 disabled parent fallback | highest(C)=`["A"]`, highest(B)=`["A"]`, highest(A)=`[]` (A omits `disabled` → strict self-test excludes it) | :553 |
| D-03 disabled trigger (ISSUE-017 shape) | `getStartNode()` = **null**; `getStartNode("Code")` = **"Code"** (destination fallback) | :824/:839/:853 |
| D-04 omitted-key asymmetry | self X→`[]`; Z→`["Y"]` (parent included); V→`["W"]` (loose parent fallback) | :498 vs :553 |
| D-05 starting order + chat skip | manualChatTrigger skipped → **"Form"** (formTrigger) | :833-855 |

The verify mode of the harness re-observes and diffs — a corrupted expected.json (seeded with the
ISSUE-017 bug) exits 1; restored via UPDATE=1 and re-verified.

## Reconstruction + falsifiability

- `start-node-navigation.ts` mirrors the reference oddities verbatim: STRICT `disabled === false`
  self-test (:498) vs LOOSE `!== true` parent fallback (:553), single-node `!node.disabled`
  (:824), manualChat skip + `STARTING_NODE_TYPES` order scanning the **full** node map (:847),
  destination fallback to `nodes[nodeNames[0]]` (:876-881), non-existent-node connection skip.
  Exported from the lane index; constants `STARTING_NODE_TYPES` / `MANUAL_CHAT_TRIGGER_LANGCHAIN_NODE_TYPE`
  pinned to `constants.ts` :53-59/:87.
- Consumer test deep-equals the reconstruction against the observed golden per probe (8 new tests:
  harness verify-mode + 5 probes + N1 + N2).
- **N1 negative control**: an asymmetry-normalising mutation (`!== true` everywhere — the exact
  Rust-port bug of ISSUE-017) is asserted to NOT match the golden. **N2**: constant-order pin.
  The D-03 golden row (`getStartNode()` → null) is precisely what fails for a port that returns the
  disabled trigger — the fixture now *predicts* the divergence instead of describing it post hoc.

## Matrix

workflow-model-lego **34/34** (build clean) · `verify:all` **real exit 0** (8 lanes) · activation
differential **65/0** · conformance + boundary unchanged-green (isolation:check inside verify:all).

## ISSUE-015/017 disposition

The audit row "the D-04 prediction currently has no executable golden on this branch" is closed for
the JS/TS track. The Rust port owner (out of this task's lane) can consume the same
`expected.json`; ISSUE-017 remains OPEN for the Rust fix, now with a portable executable oracle.
Scope warning preserved in `case.json`: plain traversal must NOT assert disabled-filtering.
