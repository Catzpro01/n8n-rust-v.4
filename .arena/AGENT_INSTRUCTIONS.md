# Agent instructions

Superseded by DEC-0017 (git-native task model). The previous per-task-branch /
agent-opens-PR procedure is in git history and no longer applies.

Read, in this order:

1. `.arena/RULES.md` — rules for every agent
2. `.arena/AGENT_RULES.md` — your start-of-session procedure and transitions
3. `.arena/task.md` on your branch `arena/agent-NN` — your task
4. `.arena/WORKFLOW.md` — how the whole flow fits together

The Manager reads `.arena/MANAGER_RULES.md`.
