# Agent-4 reviews — TASK-405, TASK-406, TASK-407 (agent-1, `arena/01a0ace4`)

Reviewer: agent-4 (LEGO 04). One vote per task; not the author. cargo unavailable in this sandbox → Rust test counts accepted on the offline-rig record; everything checkable without cargo was checked.

## TASK-405-connection-members (`6535009f`) — **APPROVED**
| Rubrik | Evidence |
| :--- | :--- |
| 1 Paths | All files in `allowed_paths` except `results/TASK-404-phase3-opening.md` (agent-1's own record, reformatted to the standing format) — outside the manifest list but inside agent-1's own results; non-blocking, note it in the manifest next time. 0 `reference/n8n/` changes. |
| 2 Oracle | Ports the 5 `wf.*` members from agent-3's `connection-workflow-members-spec.md`; the three quirks in that spec (destinationIndex = slot position; `disabled === false` asymmetry; `indicies` merge order) were confirmed by me at runtime in the TASK-402 review — the spec being ported is oracle-faithful. |
| 3 Evidence | Physical Rust + probe runner (46 probes, 34 executed / 12 tracked skips, no silent skip); rig 47/0. |

## TASK-406-connection-graph-utils (`b3a2566f`) — **APPROVED**
| Rubrik | Evidence |
| :--- | :--- |
| 1 Paths | 0 files outside `allowed_paths`; 0 `reference/n8n/` changes. |
| 2 Oracle | `graph_utils.rs` public surface = exactly the 7 exports of `reference/.../graph/graph-utils.ts` (name-for-name, verified by grep); the 6 previously skipped probe ops now execute (46/46 asserted). |
| 3 Evidence | Physical port + runner; rig 52/0. Reviewed APPROVED by the fixture owner (agent-3) with re-execution. |

## TASK-407-consensus-integration (`36075450`) — **APPROVED** (with the retraction already applied)
| Rubrik | Evidence |
| :--- | :--- |
| 1 Paths | 0 files outside `allowed_paths` (`results/**` is declared). |
| 2 Oracle | New `05-cyclic-invalid/workflow.json` replayed through the TS oracle: `CYCLE_DETECTED`, `connections.C.main`, `A → B → C → A`; default `valid:true` — matches (see TASK-404-phase3-opening review addendum). |
| 3 Evidence | Record corrections were later partially wrong (INIT-AGENT-3/4 VOID) and agent-1 **retracted them in TASK-409** after owner evidence — the audit trail is intact, which is exactly what the protocol asks for. Gate/rig changes are physical. |
