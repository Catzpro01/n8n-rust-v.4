# Agent-4 review — TASK-412-dual-phase-issue-029-030 (agent-1, `arena/01a0ace4` @ `981487ba`)

Reviewer: agent-4 (LEGO 04). Single vote; not the author.

## Vote: **APPROVED**

| Rubrik | Evidence (executed by agent-4) |
| :--- | :--- |
| 1 Paths | 10 files, **0 outside `allowed_paths`**; `contracts/workflow.contract.md` is declared in the manifest and edited only to add the D-09 declaration. 0 `reference/n8n/`, `packages/`. |
| 2 Oracle | Re-ran the engine side of `tests/differential` on real n8n-workflow 2.9.1 → **38 cases**. Engine answers I observed: `cycle-start`/`three-cycle-start`/`tail-into-cycle-start → "A"`; `nodest-chain → null`; `nodest-first-disabled → null`; `all-parents-disabled-start → "C"`; `unknown-node-start → {error: TypeError}`. Every one matches the values pinned in `unknown_node_semantics.rs` and the record. D-09 (port returns `None`/`[]` where the engine throws an incidental `TypeError` from an unguarded `this.nodes[name].disabled`) is **declared in the contract, not hidden**, with the 37/38 + 1 declared divergence tally — this is the correct handling under the truth-priority rule. |
| 3 Evidence | Physical: permanent cargo pin test, differential cases extended 14→38, gate wiring. Port side accepted on the rig record (no cargo here). |

Note from LEGO 04's side: D-09 does not touch validation — `DanglingConnections` already rejects unknown node names before any traversal is asked about them, so validation + port compose without ever hitting the engine's incidental throw.
