# 16 - Realtime UI Push Subsystem Anatomy

## 1. Mechanism
- Server-Sent Events (SSE) or WebSockets via `/rest/push`.
- Sends execution progress ticks, node completion indicators, live canvas canvas updates.
