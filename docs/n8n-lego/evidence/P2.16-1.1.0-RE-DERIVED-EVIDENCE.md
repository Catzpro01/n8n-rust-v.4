# P2.16 1.1.0 — RE-DERIVED Evidence (Agent Machine)

**Milestone:** P2.16 · **Contract:** `ai.agent-machine@1.1.0` (additive over the locked 1.0.0 publication)
**Branch:** `arena/01a0c9d3-n8n-rust-v-4` (base `99f4b01526381001e2ffb8f2323cde16b7f2c691`) · **main:** `973d3693aab3eb645ea5d6c17de749d52ab7c2f6`
**Status:** IN-PROGRESS — READY_FOR_MANAGER_REVIEW · **Merge authority:** Manager only · **P2.17:** NOT STARTED

## 0. RE-DERIVED — never recovered

The original six local-only commits (`aaccadfe`, `627ddab0`, `910919ef`, `3396eca3`, `e064fb38`,
`504216ed…`) were verified **unavailable on GitHub** (direct fetch rejected: `not our ref`) and
unavailable in any surviving local clone (`.git` loss incident). This pass is therefore a
**re-derivation from the frozen P2.16 1.1.0 specification and the locked 1.0.0 publication** — it is
**not** a byte-for-byte recovery and does not claim to be. The surviving extracted 1.1.0 contract-lock
row (`surviving-1.1.0-lock-row.json`) was preserved and is used verbatim as the single lock row.

## 1. Contract surface (Manager audit checklist)

| Check | Result |
| --- | --- |
| Contract version | `ai.agent-machine@1.1.0` |
| Lock rows | exactly **one** (`ai.agent-machine`, 1.1.0, status `implemented`) |
| Operations | **9** — create, describe, **prepare**, start, step, pause, resume, **delegate**, cancel — order matches the lock row |
| `close` | **absent** everywhere (module, manifest, lock row, capability registry, frontend vocabulary) |
| Permissions | **5** — `ai:agent:create`, `ai:agent:invoke`, `ai:agent:control`, `ai:agent:read`, `ai:agent:delegate` |
| Exports | **23** (matches surviving row exactly) |
| Fields | **19** (1.0.0's 15 + agentId, capabilityScope, delegations, startedAt declared) |
| Lifecycle states | **8** — created, **ready**, running, waiting, paused, completed, failed, cancelled; created→running (1.0.0 flow) unchanged; created→ready via prepare |
| Identity | machineId / agentId / taskId — three distinct required identities |
| Capability scope | registry-validated (lego-foundation capability registry), ≤32, unknown/duplicate fail-closed |
| Delegation | bounded bookkeeping: narrowed grants ⊆ capabilityScope, child budget clamped to parent, deadline clamped to parent deadline, maxChildren (undeclared = 0 edges), no inheritance, no child record, no execution |
| Graph validation | sequential/parallel/branch/fan-out/fan-in/join/retry; ceilings 128 nodes / 16 deps / 8 attempts; cycles, self-deps, dangling refs, multi-entry, unbounded fan-out and permission-bearing nodes rejected; input never mutated |
| Events | six session-scoped reference-only types: agent.created/started/delegated/completed/failed/cancelled; deterministic; no metadata/payload/secret/reasoning |
| Multitasking classification | MODEL = IMPLEMENTED · GRAPH SEMANTICS = IMPLEMENTED · PRODUCTION RUNTIME = **NOT IMPLEMENTED** |

## 2. Tests (local runs — debugging evidence only; official validation = Windows self-hosted runner)

| Suite | Result |
| --- | --- |
| Backend focused `lego-agent-machine.test.mjs` | **57 / 57** pass |
| Frontend focused `36-agent-machine.test.mjs` | **11 / 11** pass |
| Contract ladder (workspace + memory + context-session) | **59 / 59** pass |
| Full backend `apps/n8n-lego/test/*.test.mjs` | **552 pass / 3 fail of 555** |
| Full frontend `packages/frontend-lego/test/*.test.mjs` | **414 pass / 0 fail / 1 skip of 415** |

**Pre-existing / environmental (not caused by this change, not hidden):** the 3 full-backend failures
are the known REST node-catalog baseline: `GET /rest/types/nodes.json`, `GET /rest/types/node-versions.json`,
`POST /rest/node-types` — the catalog is fetched per install (`npm run lego:catalog`) and is not committed;
identical to the pre-change baseline (3 environmental 404s). The 1 frontend skip is the pre-existing skip.

Related suites re-run green after the manifest edits: ai-foundation 54/54, ai-set 46/46, skill 38/38,
capability-contract 33/33, foundation 24/24, boundary 22/22, milestones 13/13, milestones-context 46/46,
vocabulary 10/10, knowledge 11/11.

## 3. Gates

| Gate | Result |
| --- | --- |
| `npm run lego:arch` | OK — 26 domains, 20 locked contracts, every import respects its boundary |
| `npm run lego:arch:selftest` | **26 / 26** |
| `npm run lego:foundation` | OK — 26 LEGOs, 12 node routes |
| `npm run lego:foundation:selftest` | **15 / 15** |
| `npm run lego:capabilities` | OK — 23 REST features vs 86 registered capabilities |
| `npm run lego:scaleout` | OK — every finding is a declared, owned exception |
| `npm run lego:ai` then `npm run lego:ai:check` | OK — **64 generated / 37 curated / 101 total**, `.ai/` in sync |

## 4. XA-21 and boot payload

| Check | Result |
| --- | --- |
| XA-21 pin | **4,096 KB — untouched** (decision file and capture script not edited) |
| Assembly heap (capture) | **3,954 KB** of 4,096 KB — **PASS** (run-to-run noise ±70 KB) |
| Assembly time | **48.8 ms** of 250 ms — PASS |
| Boot payload | byte-identical **18,126 B** (asserted by `test/34` in the full frontend run) |
| Capture evidence | **56 / 56 PASS** → `docs/n8n-lego/evidence/frontend-boundary-p25.json` |

## 4b. Official CI on PR #52 (Windows self-hosted runners — the official validation)

Run for head `a1f919336ae042439888e83e18a3d52f9aca68a7` (9 check runs):

| Check | Conclusion |
| --- | --- |
| Backend LEGO architecture gate (P2.6) | ✅ success |
| Architecture, Contract & Scope Audit | ✅ success |
| Runtime gate (typecheck + unit + real-process suite + single-engine audit) | ✅ success |
| Unit + integration tests and release package | ✅ success |
| Level 2 Workspace & Node Conformance Tests | ✅ success |
| Rust workspace untouched by this change set | ✅ success |
| Windows worker portability probe (runtime tests are Linux) | ✅ success |
| Clean clone → start → health → browser smoke → restart | ✅ success |
| Level 0 (Check & Format) + Level 1 (Affected Tests) | ❌ **failure — PRE-EXISTING** |

**Classification of the single failure: `PRE-EXISTING / OUT-OF-SCOPE`.** Proof:

1. the same check already failed on the **base commit `99f4b015`** (pre-dates all six commits of this PR);
2. `Rust workspace untouched by this change set` **succeeded** on the PR head — the failing `cargo fmt --check` diffs are in `crates/n8n-workflow/src/runtime/*`, and **zero** of this PR's 26 files are under `crates/`;
3. fixing it requires touching the Rust workspace, which is outside the authorized P2.16 boundary (foundation-only, no production runtime parts) and would flip the green "Rust workspace untouched" check.

Therefore it is **not** claimed fixed and **not** hidden; the merge-policy decision (waiver / separate Manager-authorized `cargo fmt` chore / re-run) belongs to the Manager. Documented on the PR: comment on PR #52 (CI classification).

## 5. Governance

- `docs/n8n-lego/milestones.json` — P2.16 registered **in-progress** with startEvidence,
  currentCommitReference, nonScope, reconciliation, verificationEvidence; `finishEvidence` stays
  `null`; `currentMilestone` stays `P2.16` (no advancement).
- `docs/n8n-lego/BACKEND_LEGO.md` — contract changelog row added for 1.1.0.
- `.ai/` regenerated only through `npm run lego:ai` (no hand-edited generated artifacts).
- XA-21 pin not modified. No P2.17 content is implemented; roadmap is planning-only.

## 6. Scope boundaries held

No model inference, no LLM provider, no MCP, no transport kernel (P2.18), no artifact/approval
system (P2.19), no runtime adapter (P2.21), no shell/filesystem/process/network authority, no
credentials, no secret material, no hidden reasoning, no child spawning, no production scheduler.
Delegation and graph support are bookkeeping and validated representation only. Local LEGO
communication stays CALL/EVENT/STREAM/BATCH as declared. Branch hygiene: no force-push, no history
rewrite, no branch deletion, no merge — Manager owns merge.

## 7. Commits / remote head / PR

| # | SHA | Message |
| --- | --- | --- |
| 1 | `98bd401f` | feat(agent-machine): add 1.1.0 contract foundation |
| 2 | `944dfb4b` | feat(agent-machine): implement 1.1.0 semantics |
| 3 | `858078c5` | test(agent-machine): add 1.1.0 contract coverage |
| 4 | `b736e665` | chore(n8n-lego): sync governance register and .ai pack |
| 5 | `e4d1989d` | docs(n8n-lego): record P2.16 1.1.0 re-derived evidence |
| 6 | `a1f91933` | docs(n8n-lego): plan the P2.17-P2.26 roadmap (planning only) |
| 7 | _(this commit)_ | docs(n8n-lego): record official CI classification for PR #52 |

**Remote head:** see `refs/heads/arena/01a0c9d3-n8n-rust-v-4` (push-verified after each commit).
**PR:** [#52 — P2.16 Agent Machine 1.1.0](https://github.com/Catzpro01/n8n-rust-v.4/pull/52) (base `main`, open).
**Merge:** NOT PERFORMED — Manager only. **P2.17:** NOT_STARTED.
