# P2.14 — Agent 1 (frontend) final report

**Milestone:** P2.14 Memory — frontend consumption of the canonical `ai.memory@1.0.0`
**Agent:** agent-1, frontend side
**Branch:** `arena/01a0c90c-n8n-rust-v-4`
**Baseline (protected main):** `67e638ef83028bbc69876e2e768181415c7554fa` — *read only, never modified, never merged into*
**Commits on this branch:** `9fde17ec` (vocabulary + Memory surface + assembly + A29 + tests) →
`1f9046e3` (curated frontend docs re-baselined) → `b8bcd2d1` (evidence + this report) →
`b97fb469` (merge-robust ladder assertion, generated-matrix coupling, peer tip record) → this
report's update commit
**Peer (backend) publication:** `arena/01a0c90d-n8n-rust-v-4` — read at `f11aee01` (implementation
`5fbaf934`), tip now **`2dcd8570`** (doc-coupling tests + an `XA-12` arbiter string; no contract file
touched, so the quote stands at both)
**Evidence artifact:** `docs/n8n-lego/evidence/frontend-memory-p214.json` (captured at `b97fb469`)
**Reported:** 2026-09-22
**Status of this report:** an agent branch is not the milestone. **Agent completion is not merge
approval.** RECONCILIATION PASS and MERGE PASS are the Manager's, and neither is claimed here.

---

## 1. What was delivered

| Deliverable | Where | State |
|---|---|---|
| Quoted vocabulary for the published contract | `packages/frontend-lego/src/vocabulary.mjs` | **nine new quoted sets with provenance** (`memoryScope` 5, `memoryKind` 6, `memoryRetention` 5, `memoryField` 13, `memoryLifecycle` 2, `memoryGraphNode` 10, `memoryGraphEdge` 11, `memoryOperation` 4, `memoryPermission` 2); `aiFoundationCapability` 12 → **13** (`ai.memory` joined the registry list), `aiPermission` 24 → **26** (`ai:memory:read`, `ai:memory:write`); **64 quoted sets**, **29 local**, **0 pending publication**; four declared overlaps added so `vocabularyConflicts()` stays green |
| Five local states, declared with `mapsTo: null` and `decision: XA-12` | same file | `memoryListState`, `memoryPersistenceState`, `memoryOrigin`, `memoryAffordance`, `memoryRefusal` — each quoting a symbol `src/memory.mjs` really exports (`test/34` checks the citation resolves, because a provenance entry pointing at a name no module exports is a citation to nothing) |
| The Memory surface | `packages/frontend-lego/src/memory.mjs` (new) | separation from Context/Session in **both** directions, record validation against the quoted shape, the published `memory.list` contract with **four** states, three persistence words, provenance honesty, scope isolation, integrity envelope, refusals with reasons, `memoryDrift()` |
| The Memory catalog | `packages/frontend-lego/manifest/memory.json` (new) | one contract, one LEGO (`memory`, phase B), `records: []`, 13 rules, rendering flags, `publication` (this tree: `declared-not-locked`, no rows; the peer row verbatim beside it), `alignment` naming XA-12 without resolving it |
| Assembly wiring | `src/manifests.mjs`, `src/lego.mjs`, `index.mjs` | catalog validation at load; `createFrontendLego({ memory })`; `get memory()` builds the view **on demand**; `describe()` reports `memoryScopes/memoryKinds/memoryPublished/memoryDrift`; 420 exports |
| The fifth concept derived, not asserted | `src/context-session.mjs` | the Memory concept is derived from the rows handed over, `contextSessionPublication()` exposes `memoryPublished`, and a record that carries a memory payload is still refused by name |
| Conformance rule **A29** + contract prose | `src/conformance.mjs`, `contracts/frontend.contract.md` §19.20 | 29 rules, 29 live checks, `ok: true`; the JSON rule block parses to exactly `ruleIds()` (A1..A29) |
| Tests | `test/34-memory.test.mjs` (new, 26), `test/29-alignment.test.mjs`, `test/32-context-session.test.mjs` | see §3 |
| Evidence capture | `packages/frontend-lego/scripts/capture-memory-evidence.mjs` (new) | one command, one artifact; reads the peer tree only through `N8N_BACKEND_LEGO_ROOT`; runs the boundary capture with `--out` into a temp path so the pinned `frontend-boundary-p25.json` can never be overwritten by a degraded sandbox run |

**Contracts, capabilities, operations, permissions and error vocabularies published by this branch:
none.** The frontend publishes no contract, registers no capability, adds no operation and reuses the
shared `lego.*` error vocabulary. It consumes `ai.memory@1.0.0` and redefines nothing.

---

## 2. The four reported drifts — audited, then resolved

Agent-2's milestone block reported the frontend at **358/362 with four expected `ai.memory` drifts**. Each
one was audited before it was touched, and each resolution is recorded with the reason.

| # | Reported drift | Verdict after audit | What changed |
|---|---|---|---|
| 1 | `aiFoundationCapability` drift for `ai.memory` | **stale assertion, not a backend defect** — the quoted list predated the publication and no rule existed for a publication the lock was pinned before | `aiFoundationCapability` carries `ai.memory`; `aiPermission` carries the two words; `test/29` gained a third publication group (`MOVED_BY_P214_PUBLICATION`, eleven sets — two of them already moved by P2.12) and asserts the non-comparison bounds **in both directions** |
| 2 | future Memory ladder expectation | **obsolete expectation, documented in place** — `test/32` asserted a flat denial (`exists === false`, "no `ai.memory` contract is cited, because none exists"), which is true of protected main and false of the tree that publishes Memory | the concept is now **derived** from the rows handed over; the contract stays named in both trees; every refusal is kept (`assertDistinctConcepts()` still refuses a record that carries a memory payload, with `frontend.context-session.concepts-merged`, and the Memory half refuses the mirror image). The test is strictly stronger, and the reason is written into the test |
| 3 | P2.14 reconciliation shape | **stale manifest + stale assertion** | `manifest/context-session.json` listed `ai.memory.*` as unpublished; it now names only the two names that really are unpublished (`ai.memory.traverse`, `ai.memory.relate`), and `test/32` pins that bound in both directions (no name of the form `ai.memory.<published verb>` may appear in an unpublished list). The Memory catalog records the publication on both sides without claiming either |
| 4 | one assertion/manifest alignment still referring to the pre-agent-2 state | **stale publication pointer, not a defect** | `test/32` asserted `publishedOnPeerBranch.onProtectedMain === false` — true while P2.13 was in flight, false since the manager merged it as PR #45. It now requires the claim to be *backed*: `onProtectedMain` true **and** the merge named **and** the protected-main block published. A bare "published" claim still fails |
| 5 | *(not reported by agent-2)* the curated frontend card did not name the new module | found by the frontend suite itself | `.ai/frontend/card.md` names `src/memory.mjs` and `test/34-memory.test.mjs`, with the numbers it publishes corrected to the measured ones (390 tests across 34 suites, 29 rules, 64/29 vocabularies) |
| 6 | `2dcd8570` follow-up — the future Memory ladder, measured precisely, plus the generated matrix | see §3b: one real failure on the merged tree (latent here), one coupling check rewritten for the tree under test | the ladder assertion now holds for both registers and is stronger; the generated matrix is coupled to *this* tree's lock instead of one tree's fixed value |

No test was deleted or weakened to obtain a pass. Every change above is either a tighter assertion or
the same assertion against the state the contract actually publishes; where an old expectation became
false, it was replaced by the derivation that is true in **both** trees and the reason was recorded in
the test.

---

## 3. Measurements

All numbers below were produced by this branch's own commands; `N8N_BACKEND_LEGO_ROOT` points the
backend comparison at a read-only unpack of agent-2's branch (`/tmp/peer214/...`).

| Run | This tree (protected main @ `67e638ef` backend) | Peer tree (agent-2 @ `f11aee01`) |
|---|---|---|
| Memory-focused suite (`test/34`) | 28 tests — 26 pass, 0 fail, **2 skipped with a stated reason** | 28 tests — 27 pass, 0 fail, **1 skipped with a stated reason** |
| Focused suites (`12, 19, 24, 29, 32, 33, 34`) | 121 tests — 119 pass, 0 fail, 2 skipped | 121 tests — 120 pass, 0 fail, 1 skipped |
| **Full frontend suite** (`frontend-lego:test`) | **390 tests — 388 pass, 0 fail, 2 skipped** | **390 tests — 389 pass, 0 fail, 1 skipped** |
| Merged-tree simulation (agent-2 backend + register, this frontend) | **390 tests — 389 pass, 0 fail, 1 skipped**; backend memory suite 20/20; `lego:ai:check` / `lego:arch` / `lego:capabilities` / `lego:foundation` green | — |
| Backend suite (`lego:test`) | **454/457** — the 3 failures are the node-catalog REST suites | n/a |

The skips are the two halves of the alignment test: against a tree that publishes no `ai.memory` row
the comparison is **announced as a bounded non-comparison** (exactly the eleven sets the publication
moved, asserted in both directions), and against a tree that does, every one of them is compared for
real. Neither direction reports a pass it did not perform.

`lego:test`'s three failures were reproduced on a **pristine `git archive main` tree**
(`apps/n8n-lego/test/rest.test.mjs`: 9 pass / 3 fail there too) — pre-existing and environmental: the
8 MB node catalog is not committed, so those routes answer 404.

### Gates

| Gate | Result |
|---|---|
| `npm run lego:arch` | **PASS** — 26 domains, 17 locked public contracts, every backend import respects its declared boundary |
| `npm run lego:arch:selftest` | **PASS** — 26/26 |
| `npm run lego:foundation` | **PASS** — 26 LEGOs, 12 node creation routes |
| `npm run lego:foundation:selftest` | **PASS** — 15/15 |
| `npm run lego:capabilities` | **PASS** — 23 REST features vs 83 registered capabilities, owners and phases agree |
| `npm run lego:scaleout` | **PASS** — every finding is a declared, owned exception; no new process-local coupling |
| `npm run lego:ai:check` | **PASS** — `.ai` in sync: 64 generated, 37 curated, 101 total (no generated file hand-edited) |
| `npm run frontend-lego:test` | **PASS** — 388 tests, 0 failures (both trees, above) |
| `npm run lego:gate` | **not claimed as a single PASS**: the chain includes `lego:test`, whose three environmental failures are pre-existing and reproduced on pristine main. Every other link in the chain passes, and `frontend-lego:test` passes standalone |
| Sub-LEGO audit (`tools/sublego-audit/audit.py`) | **BLOCKED** — `ModuleNotFoundError: No module named 'yaml'` in this sandbox; reported as *unmeasured*, never as passing |
| Boot payload verification | **PASS** — 18,126 bytes, byte-identical to the pinned P2.5 baseline; no memory word travels in the descriptor (`test/34` asserts this directly) |
| Boundary capture (`capture-frontend-evidence.mjs`, `--out` to a temp path) | **53/56** and **52/56** on two runs — the failures are the three page-level checks that need the stock n8n editor UI dependency (absent here), plus, in one run, the heap sample below. The pinned `frontend-boundary-p25.json` was **not overwritten** |

### XA-21 — the descriptor-assembly heap pin (status: **open, untouched**)

The 4,096 KB pin was **not edited** and is not claimed to have passed by lowering anything:

| Measurement | Value |
|---|---|
| Boundary capture sample (inside a just-finished capture run) | **4,958 KB → FAIL** (recorded) |
| Three isolated samples (same code, quiet process) | **3,814 / 3,806 / 3,811 KB** |
| P2.13 historical samples (recorded in the P2.13 evidence) | 4,275 KB, 4,444 KB |

The number moves with the sandbox: a single `heapUsed` sample around a cold import is noisy, so the
evidence artifact records **every** sample including the one above the pin rather than a favourable
one. The Memory view is built on demand, adds no descriptor entry and does not change the boot
payload, so this milestone introduces no deliberate growth; whether the pin itself needs raising
remains **the Manager's** ruling (XA-21).

---

## 3b. Second round — agent-2's follow-up commit and the merge

Agent-2 published `2dcd8570` on top of `f11aee01`: backend doc-coupling test (19 → 20 in
`lego-memory.test.mjs`), a five-test **frontend** copy of `packages/frontend-lego/test/34-memory.test.mjs`,
and an `XA-12` `arbiter` string. Three things were done about it rather than waiting for the merge to
find them.

### 3b.1 The merge was simulated, and it had exactly one frontend failure

The tree the manager will produce (agent-2's backend and register + this branch's frontend) was built
from `git archive 2dcd8570` with this branch's frontend files overlaid, and the suite was run there:

| Run | Before the fix | After the fix |
|---|---|---|
| Frontend suite, merged tree | 388 tests, 386 pass, **1 fail** | **390 tests, 389 pass, 0 fail**, 1 skip |
| Backend memory suite, merged tree | 20/20 | 20/20 |
| `lego:ai:check` / `lego:arch` / `lego:capabilities` / `lego:foundation`, merged tree | green | green |

The failure was `test/32 — the register keeps the strategic phases, the ladder and the merge
protocol`, on `future.some((milestone) => /Memory/.test(milestone.title))`: it demanded a **planned**
milestone titled Memory, which agent-2's register edit (P2.14 Memory → `in-progress`) makes false. It
was *latent* here — the branch carries protected main's register copy, so the test passed — and real
on the merged tree. This is the drift agent-2 attributed to "the future Memory ladder", and the
assertion is now stronger and true of both registers: Memory is named on the ladder, its id is
`P2.14`, its status is the register's real one, it is **not** claimed `complete`, `currentMilestone` is
a milestone being worked on, and the milestones nobody has started (Workspace, Agent Machine,
P2.17+) are still `planned`.

### 3b.2 Agent-2's frontend numbers, measured

| Claim | Measured here |
|---|---|
| "365/367 pass, 2 fail" | **367 tests, 365 pass, 2 fail** in the peer tree's default environment — the count reproduces exactly |
| — | 367 tests, **364 pass, 3 fail** when the peer tree is pointed at its own backend (adds the P2.13 publication-pointer assertion) |
| the two remaining drifts | correct in substance: `test/29` (`aiFoundationCapability`) and `test/32` (the register ladder). The second is the *register* assertion, not the "no Memory store" one; the flat count alone does not distinguish them |
| "`XA-12` arbiter was null, which made `24-vocabulary` fail" | true of **their own branch**: `f11aee01` set `arbiter: null` on a still-open row, and `test/24` line 196 requires a string — measured 9/10 in their tree at that commit. Protected main and this branch carry a manager string and pass 10/10 throughout; `2dcd8570` restores it and their tree is 10/10 again. **Self-introduced and self-fixed on the peer branch**, not a pre-existing defect |
| pristine `main` baseline | **362 tests, 362 pass** (`git archive main`) — so none of the failures above is inherited |

### 3b.3 Doc coupling for the generated matrix, and the same-path collision

- Agent-2's frontend copy of `test/34` asserts `.ai/master/AI_CONTRACT_MATRIX.md` says
  `ai.memory … locked @ 1.0.0 / IMPLEMENTED`. That file is **generated** from the tree's manifests and
  lock, and this tree publishes no `ai.memory` row — so its matrix correctly says
  `publicationPending` / `PLANNED`. A fixed value would copy one tree's state; the check added here
  asserts the **coupling**: the file is GENERATED, it carries a Memory row, and that row says
  `locked @ <version>` exactly when *this* tree's lock carries the row and `publicationPending`
  exactly when it does not. It passes on this tree, on the peer branch and on the merged tree, and
  fails on a hand-edit or a lock/matrix drift. Generated output was **not** hand-edited.
- **Both branches add `packages/frontend-lego/test/34-memory.test.mjs`** — 26 tests here (the Memory
  surface suite, which is A29's `enforcedBy`), 5 on the peer branch (backend-side doc coupling). A
  same-path add is silent until it conflicts, so it is now named in `manifest/memory.json`
  (`publication.publishedOnPeerBranch.collision`) and in the vocabulary lock, together with the rule
  that the frontend path stays the frontend suite and the peer checks worth keeping are already
  asserted here against the tree under test. **The reconciliation of that one file is the Manager's.**
- The peer tip and what it changed are recorded (`tip: 2dcd8570`, `tipChange`) beside the commit the
  words were read from (`f11aee01`): provenance records where a value came from, not where a branch
  happens to be now. Verified: no contract file, lock row, manifest value or registry entry changed
  between the two commits.

## 4. XA-12 — status

**`XA-12` remains `open-for-manager`. This branch does not resolve it, does not edit it and does not
imply its resolution.**

- What the decision owns and what this milestone consumed: `ai.memory@1.0.0` — the bounded surface the
  decision asked for — is published and consumed: four operations, two permission words, provider
  boundary, scope isolation, checksum integrity, explicit terminal forgetting.
- What stays open, verbatim from the register's own row and from the peer manifest's `openDecisions`:
  **relevance-ranked traversal**, **explicit edge creation (`relate`)** and **retention-policy
  enforcement**. `traverse` and `relate` are declared future stages and are *named and refused* here
  (`DEFERRED_MEMORY_OPERATIONS`, `MEMORY_AFFORDANCES.forbidden`, `graph.traversalPublished: false`).
- The frontend's rendering of the open half: no search box, no score, no similarity, no countdown, no
  restore, no silent injection of memory into context.
- `manifest/memory.json#alignment` names the decision, its register, its status and this branch's test
  file; `test/34` asserts the register row is still `open-for-manager` and that the frontend does not
  claim otherwise. `XA-11` (where Skill/Memory are modelled) is named beside it and unchanged: no new
  domain and no new LEGO was created — `ai.memory` stays a capability of `ai-foundation` and `memory`
  stays index 4 of the AI set.

## 5. Synchronization

| Direction | State |
|---|---|
| agent-2 → agent-1 | **CONSUMED**: fetched (`f11aee01`), inspected (manifest, surface module, tests, lock row, registry entry, evidence), quoted with provenance, and compared for real against the peer tree in `test/29` and `test/34` |
| agent-1 → agent-2 | **SYNC_REQUIRED**: agent-2 has not yet fetched this branch. The vocabulary lock, the registry lists (`aiFoundationCapability`, `aiPermission`), the Memory catalog and the conformance rule count are contract/governance-shaped changes, so agent-2 (or the Manager) must consume them before the milestone can be reconciled |
| Shared files | The milestone register (`docs/n8n-lego/milestones.json`) and `docs/n8n-lego/decisions/cross-agent-decisions.json` were **not touched by this branch**: agent-2 already carries the P2.14 register row and the XA rows on its branch, and two branches rewriting the same governance block is the collision the protocol exists to prevent |
| Peer defect reported, not fixed | `docs/n8n-lego/evidence/frontend-boundary-p25.json` was overwritten by a degraded sandbox capture on agent-2's branch (three PASS verdicts became FAIL and the baseline block moved). Recorded in `manifest/memory.json#publication.publishedOnPeerBranch.knownPeerDefect` so the reconciler restores the pinned artifact instead of merging the overwrite |

---

## 6. Dangling work, out-of-scope observations, non-claims

**Dangling work for this milestone's frontend half: zero.**

The following are *not* agent-1's to close, and are named so nobody mistakes them for silence:

1. **Manager reconciliation** of the two draft branches (the vocabulary and registry lists are
   duplicated per branch by design and must be reconciled, not averaged); then MERGE PASS and
   post-merge verification. One file needs an explicit resolution because **both** branches add it:
   `packages/frontend-lego/test/34-memory.test.mjs` — 26 tests here (the Memory surface suite and
   A29's `enforcedBy`), 5 on agent-2's branch (backend-side doc coupling). Keep the frontend suite;
   the peer checks worth keeping are already asserted in it against the tree under test. The
   simulated merge of the two branches is green (390 tests, 0 failures).
2. **The canonical milestone register** — agent-2 carries the P2.14 row (`in-progress`,
   `reconciliation` block, backend evidence). This branch deliberately adds no second edit and no
   second register; frontline evidence is `docs/n8n-lego/evidence/P2.14-agent-1-final-report.md`.
   Note for the reconciler: `test/33` currently pins `currentMilestone` to `P2.13`, so the register
   edit that moves it to P2.14 and that assertion must land in the same change.
3. **Pre-existing defects observed, out of P2.14 scope, reported not fixed:**
   - `LOCAL_VOCABULARIES` entries `installationLayer` and `aiKindRole` cite
     `src/agents.mjs#INSTALLATION_LAYERS` / `#AI_KIND_ROLES`, and **neither symbol exists** in that
     module — a provenance citation to nothing (P2.10-era). `test/34` enforces the rule for the
     memory sets only; the older two are left for their owner rather than silently "fixed" here.
   - `PENDING_CONTRACT_ROWS` keeps two P2.13 rows marked `onProtectedMain: true` under a doc comment
     that reads "**none remain** … this list is empty". The rows are deliberate publication evidence
     (a `PUBLISHED_CONTRACT_ROWS` twin exists), but the comment misstates the array; P2.13's, not
     P2.14's.
4. **Environment-blocked, pre-existing:** the sub-LEGO audit (no PyYAML), `verify:fast` (needs
   `packages/workflow-lego/node_modules`), the three node-catalog REST suites, and the three
   page-level boundary checks (stock UI dependency absent).

**Non-claims.** No merge (neither branch is merged, and this branch is not merged into `main`); no
`main` write; no force-push; no P2.15 or P2.16 work; no Agent Machine, Workspace, MCP, Runtime Adapter,
Node Creator, Translation runtime, model inference, provider integration, token provider or Rust
implementation; no backend semantics changed; no contract published; no capability registered; no
operation or permission invented; `XA-12` not resolved; `XA-21` pin not edited.

---

## 7. Reconciliation pass (manager-directed, on this branch)

The manager opened the reconciliation pass after both P2.14 branches were complete, and assigned the
register edit to agent-1 so that no two branches rewrite the same governance block. This section
supersedes §6 items 1 and 2; the rest of the report stands as written and is not rewritten.

**What this one commit changes, and what it deliberately does not.**

| File | Change |
| :--- | :--- |
| `packages/frontend-lego/test/34-memory.test.mjs` | 28 tests kept as the canonical frontend suite, plus **one folded-in test** built from agent-2's genuinely unique coverage: the publishing tree registers `ai.memory` in four places (product manifest LEGO row, capability registry, contract lock, contract document graph) and the four agree. Agent-2's fixed `locked @ 1.0.0 / IMPLEMENTED` matrix assertion was **not** carried over: the matrix is generated per tree, so that value is false on a tree whose lock has no `ai.memory` row — the coupling that holds on both trees is asserted instead. |
| `docs/n8n-lego/milestones.json` | P2.13 → `complete` on protected main (`67e638ef`, PR #45 + PR #46), its reconciliation block kept and marked historical with a `resolution`; P2.14 → `in-progress` with start baseline `67e638ef`, agent-2 evidence `2dcd8570`, agent-1 evidence `ea7c956f`, `finishEvidence: null`, `reconciliation.verdict = RECONCILIATION_REQUIRED`; top-level `currentMilestone` `P2.14`, `previousCompletedMilestone` `P2.13`, `mainBaseline` `67e638ef`, `agentBranches` the P2.14 pair (P2.13's baseline and branches stay on the P2.13 row). |
| `packages/frontend-lego/test/32-context-session.test.mjs`, `test/33-milestones.test.mjs` | The register expectations follow the register: P2.13 complete, P2.14 in progress and current, the main baseline coupled to P2.14's start evidence, and a new guard that a `complete` milestone may not carry an *active* reconciliation verdict. |
| `.ai/master/frontend/CURRENT_STATUS.md` (curated) | The current/previous milestone rows, the branch table and the baseline rows were corrected; the document keeps its P2.13 measurement baseline and says so. |
| `.ai/master/{MILESTONE_REGISTER,IMPLEMENTATION_PHASES,CURRENT_STATUS}.md` | **Regenerated**, not hand-edited, from the register. |
| `tools/lego/ai-pack.mjs` | One generated sentence was pinned to P2.13 ("P2.13 requires RECONCILIATION PASS… before it can become complete"); it now reads the register's current milestone, so it cannot go stale again. |
| `docs/n8n-lego/evidence/frontend-boundary-p25.json` | **Untouched.** The pinned protected-main artifact is preserved on this branch; agent-2's sandbox recapture is not imported. |

**Measured on the reconciliation commit.** Focused (`test/29`, `test/32`, `test/33`, `test/34`): 96
tests, 93 pass, 0 fail, 3 skipped (the publication comparisons, announced rather than passed). Full
frontend suite: **391 tests, 388 pass, 0 fail, 3 skipped**. `lego:ai:check`: OK (64 generated / 37
curated / 101). `lego:arch`, `lego:arch:selftest` 26/26, `lego:foundation`, `lego:foundation:selftest`
15/15, `lego:capabilities`, `lego:scaleout`: OK. `lego:test`: 454/457, the same three node-catalog
REST failures that reproduce on a pristine protected-main archive.

**Merged-tree simulation** (agent-2's backend @ `2dcd8570` + this branch's frontend, contract
document, reconciled register and pinned boundary artifact, with `.ai` regenerated for the merged
lock): full frontend suite **391 tests, 390 pass, 0 fail, 1 skipped** — the folded registry test runs
against the publishing tree instead of skipping, and passes — and the backend Memory suite 20/20.

**Two merge hazards the manager has to resolve, both stated in the register's P2.14 block.**

1. **The pinned artifact will be replaced silently, not conflicted.** Agent-2 modified
   `docs/n8n-lego/evidence/frontend-boundary-p25.json`; this branch did not touch it in this pass, so
   a three-way merge takes *their* recapture with no conflict marker. The merged tree must keep the
   protected-main blob `07f93d8f` (`git checkout main -- docs/n8n-lego/evidence/frontend-boundary-p25.json`
   is the one-line resolution).
2. **The generated `.ai` pack must be regenerated on the merged tree**, because the merged lock
   publishes `ai.memory` (17 rows on protected main + this row) and the generated matrix is derived
   from the lock of the tree it is generated in. Hand-editing it is the defect the freshness gate
   exists to catch.

**Status: `WAITING_FOR_MANAGER_MERGE_PASS`.** P2.14 remains **in progress** — this branch does not
merge itself, does not mark the milestone complete, and starts no P2.15 work.

---

## 8. Backend consumption (manager decision: single merge via PR #48)

The frozen backend contribution — `arena/01a0c90d-n8n-rust-v-4` @ `2dcd8570`, `AGENT_2 = FROZEN` — is consumed
into this branch by a second, clearly-labelled commit so that **PR #48 is the only P2.14 merge candidate**.
Nothing in the contribution is edited: `git diff 2dcd8570 -- <the consumed paths>` is empty.

**Consumed (byte-identical, 10 files):** `apps/n8n-lego/src/lego/manifest/memory.json`,
`apps/n8n-lego/src/lego/memory.mjs`, the `ai.memory` capability in `manifest/domains.json` and the
`memory` declaration block in `manifest/ai-foundation.json`, the product-manifest `memory` LEGO row in
`manifest/ai-lego-set.json`, the `ai.memory@1.0.0` row in `contracts/contract-lock.json`,
`apps/n8n-lego/test/lego-memory.test.mjs`, `apps/n8n-lego/test/lego-ai-set.test.mjs`,
`apps/n8n-lego/test/lego-ai-foundation.test.mjs`, `docs/n8n-lego/evidence/P2.14-agent-2-final-report.md`.

**Deliberately not consumed, with the reason:**

| Not consumed | Why |
| :--- | :--- |
| `docs/n8n-lego/evidence/frontend-boundary-p25.json` (agent-2's recapture) | The pinned protected-main artifact (`07f93d8f`) is preserved; the recapture is a degraded sandbox run and the manager's constraint 7/8 excludes it. |
| `packages/frontend-lego/test/34-memory.test.mjs` (agent-2's 5-test copy) | The frontend suite is canonical and already folds in the peer's unique coverage. |
| `docs/n8n-lego/milestones.json` (agent-2's row) | This branch carries the reconciled register; one register, one edit. |
| `.ai/**` (agent-2's generated views) | Generated output is not merged; it is regenerated with `npm run lego:ai` on the consuming tree. |
| `docs/n8n-lego/decisions/cross-agent-decisions.json` | It carries a P2.14 `XA-12` finding **and** a wide escape rewrite of unrelated rows. `XA-12` must not change in this pass, and the decision register is outside the preserve list — so it is left to the manager rather than folded in silently. |
| `docs/isolation/**` | Unrelated to P2.14, and agent-2's version records `5/10 PASS · BEHAVIOR CHANGE: ISOLATION FAILED` — importing it would import a failure claim this milestone did not produce. |

**Validated on the consuming tree:** frontend Memory suite `test/34` 29 tests / 28 pass / 0 fail / 1 skip;
focused (`test/29`, `test/32`, `test/33`, `test/34`) 96 / 95 / 0 / 1; **full frontend suite 391 / 390 / 0 / 1**;
**backend Memory 20/20 in-tree**; `npm run lego:test` 474/477 with the same three node-catalog failures that
reproduce on a pristine protected-main archive; `lego:ai:check` OK after regeneration; `lego:arch`,
`lego:arch:selftest` 26/26, `lego:foundation`, `lego:foundation:selftest` 15/15, `lego:capabilities`,
`lego:scaleout` OK. **No new failure.** Pinned artifact blob `07f93d8f`; `ai.memory` exactly one lock row at
`1.0.0`, four operations, two permissions, unchanged semantics.

**Status: `PR_READY` / `MERGE_PENDING_MANAGER`.** `P2.14` stays **in progress** until the manager merges to
protected main, verifies protected main, and moves the register (`P2.14` → complete, `P2.15` → current).
