# P2.17 — Agent Machine Runtime Foundation (evidence)

Status: implementation + local verification record for the bounded runtime over `ai.agent-machine@1.1.0`.
Official validation: PENDING — GitHub Actions self-hosted Windows runner on the PR (local runs are debug evidence only).

## Scope delivered

- `apps/n8n-lego/src/lego/agent-machine-runtime.mjs` — bounded local execution runtime:
  delegates the 9 contract lifecycle ops (never invents `close`), validates graphs with the
  contract's `validateAgentGraph`, level-scheduled execution with bounded concurrency,
  bounded retry, deadline → contract `budget-exhausted`, dedupe-key idempotency (bounded
  operation log), backpressure admission, failure isolation per machine, cooperative
  idempotent cancellation, Universal Agent Events via `deriveAgentMachineEvents`
  (correlation = machineId, sessionId passthrough, no cross-session leakage), and a
  bounded structured audit trail that never records reasoning.
- `apps/n8n-lego/test/lego-agent-machine-runtime.test.mjs` — §19 matrix (26 tests).
- Register sync: `docs/n8n-lego/milestones.json` — P2.16 `complete`
  (`finishEvidence.protectedMain = f21882233c1f4efc5bfb8f3e1b5e1ad4db781d7d`),
  new `P2.17` row `in-progress`, `currentMilestone = P2.17`,
  `mainBaseline = f21882233c1f4efc5bfb8f3e1b5e1ad4db781d7d`,
  synced assertions in `packages/frontend-lego/test/33-milestones.test.mjs` and
  `packages/frontend-lego/test/32-context-session.test.mjs`.

## Explicitly outside scope (fail-closed, no code paths)

Production external-agent runtime · shell/filesystem/subprocess executors · credential manager ·
model inference · provider implementations · MCP · universal bridge · GitHub/Telegram integrations ·
Arena runtime · approval resolution (waiting stays cancel-only).

## Frozen surfaces (must stay unchanged)

- Contract lock: single row `1.1.0`; `agent-machine.mjs` untouched by P2.17.
- `ai-lego-set` `agent-machine` LEGO entry stays `contract-only` (flip = Manager decision).
- XA-21 capture, `capabilities.json` boot pin, `.arena/registry/agents.yaml` untouched.

## Verification results (local debug evidence — 2026-09-23)

- Lego gates **7/7 PASS**: `lego:ai:check` OK (regenerated `npm run lego:ai`, 64 generated / 37 curated / 101 total),
  `lego:arch` OK (26 domains, 20 locked contracts), `lego:arch:selftest` 26/26,
  `lego:foundation` OK, `lego:foundation:selftest` 15/15,
  `lego:capabilities` OK (23 REST features / 86 capabilities), `lego:scaleout` OK (declared exceptions only).
  Runtime error codes use the published vocabulary only (`lego.backpressure`, `lego.contract_violation`) —
  `contracts/errors.contract.json` unchanged.
- Focused: agent-machine contract + runtime **83/83**; milestones + context-session **59/59**.
- Full backend (apps/n8n-lego): **578/581** — the 3 failures are `test/rest.test.mjs` node-catalog
  (404≠200), **proven pre-existing**: stashing all P2.17 tracked changes and re-running on clean
  `f2188223` reproduces the identical 9/12 with the same 3 failures (BASE==FAIL, HEAD==FAIL).
  Runtime suite contributes +26 green (555→581 total, same 3 baseline failures).
- Full frontend (packages/frontend-lego): **414/415** — 0 failed, 1 skipped (identical to baseline).
- Gateway pytest: **12 passed, 3 failed** — the documented baseline set
  (`test_zero_secret_exposure` = environmental/live-Supabase, `test_persistent_orchestrator_tick`
  = pre-existing, `test_audit_logging_integrity` = known; also fails on the untouched tree —
  `git diff f2188223 -- tools/gateway` is empty, so BASE==FAIL, HEAD==FAIL).
  Gateway token/audit artifacts removed after every run; never committed.
- Contract lock: unchanged single row `1.1.0`; `agent-machine.mjs` diff vs `f2188223` = 0.
- Worktree after gates: only the 10 intended files (3 new + 7 modified); `.arena` artifacts absent.
