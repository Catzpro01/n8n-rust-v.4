# P2.18 — Universal Transport & Envelope Kernel: evidence

- **Milestone:** P2.18 · **Phase:** B · **Owner:** manager · **Backend owner:** agent-1
- **Branch:** `arena/01a0c9d3-n8n-rust-v-4`
- **Baseline (startEvidence):** `8da4d00c7e1bca7fc69a4f8d36f59c453f96ee02` (protected main after the verified P2.17 merge)
- **Date:** 2026-09-23
- **Official validation:** the installed GitHub Actions self-hosted Windows runner (the PR checks).
  Everything below is local debug evidence, not official sign-off.

## Scope delivered

| Deliverable | Where |
| --- | --- |
| Universal envelope + transport-neutral kernel: `{envelope, payload}` as one validated shape for CALL/EVENT/STREAM/BATCH | `apps/n8n-lego/src/lego/transport-kernel.mjs` (new, `lego.transport-kernel@1.0.0`) |
| Deterministic codec: canonical JSON (recursively sorted keys, no whitespace) with proven round-trip fixed point | `encodeMessage` / `decodeMessage` |
| Schema validation on entry — malformed fails closed with `lego.contract_violation` (published code, no new error contract) | `validateEnvelope`, `KernelViolationError` |
| `causationId` envelope field (minor, additive): immediate parent request; derived default = parent `requestId` | `apps/n8n-lego/src/lego/envelope.mjs` → `lego.envelope@1.1.0`, mirrored in `foundation.json` |
| `BATCH_LIMITS` (256) + batch refuses oversize/non-array | `apps/n8n-lego/src/lego/interaction.mjs` → `lego.interaction@1.1.0` |
| Bounded `applyBackpressure`: buffer past watermark = declared `onOverflow` or typed `lego.backpressure` refusal (never an unbounded queue); `block` = synchronous `blocked` refusal + bounded `pushAsync` waiting (`MAX_BLOCK_WAITERS` = 1024); `highWaterMark` int ∈ [1, 65536] | same file |
| Contract governance: lock rows updated (`lego.envelope` 1.1.0, `lego.interaction` 1.1.0 + `BATCH_LIMITS`, new `lego.transport-kernel` row), 3 changelog rows in `BACKEND_LEGO.md` §Contract | R7 + R8 satisfied |

## Roadmap acceptance → tests (`apps/n8n-lego/test/lego-transport-kernel.test.mjs`, 26 tests)

| Acceptance (roadmap §P2.18) | Proof |
| --- | --- |
| Round-trip determinism | key-order-independent encoding; `encode∘decode∘encode` byte-stable fixed point; identity/timing fields preserved |
| Malformed rejected | truncated/`null`/array/missing halves/unknown keys/wrong types/oversize (64 KiB) → `lego.contract_violation` before any repair |
| Backpressure observed under load | 64-item burst vs watermark 4 → typed `lego.backpressure` refusals, retryable, buffer never exceeds 4; every one of the 7 policies sheds under overload; resume after drain |
| No unbounded queue anywhere | per-push watermark invariant for all policies; message byte ceiling checked pre-parse; batch ceiling `BATCH_LIMITS.maxItems`; JSON depth ceiling bounds the validator itself; blocked-producers waiter bound |

Propagation checks: correlation / causation / trace / deadline-clamp / cancellation flag / idempotency key survive `encode→decode`; an `AbortSignal` never crosses the wire (`cancellable` flag does); expired deadline still reads cancelled after the round trip; STREAM items ride the same envelope+payload shape through the kernel.

## Gates (local debug run, this workspace)

- `npm run lego:ai` regenerated, then: **ai:check ✓ · arch ✓ (21 locked contracts) · arch:selftest 26/26 ✓ · foundation ✓ · foundation:selftest 15/15 ✓ · capabilities ✓ · scaleout ✓**
- Kernel suite: **26/26 pass**. Communication suite (regression on envelope/interaction): **65/65 pass**.
- Full frontend suite: **415 tests, 414 pass, 0 fail** (1 declared skip).
- Full backend suite: **607 tests, 604 pass, 3 fail — PRE-EXISTING**:
  `rest.test.mjs` catalog 404s (`GET /rest/types/nodes.json`,
  `GET /rest/types/node-versions.json`, `POST /rest/node-types`). Verified by
  stashing this change and re-running on the committed baseline: the same 3
  fail without these changes (node-catalog artifacts, unrelated to transport →
  FOLLOW-UP, non-blocking). Official judgment stays with the Windows runner.
- NEW failures found mid-cycle and fixed before push: frontend quote
  `lego.interaction` 1.0.0→1.1.0 (`vocabulary.mjs` ×2) and the lock-row count
  pin 20→21 in `34-memory.test.mjs`.
- Security gate: no credential patterns / credential URLs across the 13 changed files.

## Non-goals honored

No internal HTTP, broker or daemon; no MCP (P2.20), no transport adapters (P2.21), no model inference,
no distributed infra, no artifact/approval (P2.19), no shell/fs/subprocess executor, no credential
manager, no GitHub/Telegram, no Arena runtime. `ai.agent-machine@1.1.0`, XA-21, `crates/**`,
frontend envelope (separate contract) untouched.
