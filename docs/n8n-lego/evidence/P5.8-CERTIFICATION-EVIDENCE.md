# P5.8 — Security Plane Certification: Evidence

Issue: #221 · Parent: #85 · Branch: `arena/p5.8-final-certification` · Baseline for comparisons: pre-P5 `main @ cc640344b1054ba98e317a4a1be91ad6a63f3638` (the first parent of the P5.1 merge, the same baseline P5.1 recorded).

Classification used throughout: **measured** (produced by running code on the machine below), **tested** (asserted by a test in CI), **inferred** (reasoned, not measured), **debt** (known, not done).

## 1. The P5 plane on `main`

| slice | issue | PR | merge commit |
|---|---|---|---|
| P5.1 security kernel | #214 | #226 | `0842a05f297b1c0fc96a3b0b65498e5c52febb49` |
| P5.2 session kernel, rotation, revocation, CSRF | #215 | #242 | `d8f18174697d6b8f3227490d28f5cce60e675b3c` |
| P5.3 authorization engine | #216 | #247 (+ evidence #248) | `57606b52ff9e19ab0b57fb90d89e81a37002e75b` |
| P5.4 credential boundary | #217 | #249 | `ae99e9808d0685425ef99cca5005cf60a2407144` |
| P5.5 KeyProvider, envelope encryption, rotation, recovery | #218 | #250 | `260b838d2ab5e5de21ffa8acaeb94316cce8a39b` |
| P5.6 password recovery, abuse control, TOTP MFA, step-up | #219 | #251 | `4e6802c84270986b519418856f95b625943edeb0` |
| P5.7 API keys, service principals, tenant policy, delegation | #220 | #252 | `5310bf31fd2517ea797432d9b86ed974a685bca0` |
| P5.8 certification (this) | #221 | see PR | recorded post-merge on #221 |

P5.1–P5.3 evidence: `docs/n8n-lego/evidence/P5.1-…`, `P5.2-…`, `P5.3-…` (unchanged). P5.4–P5.7 evidence lives in the closing comments on #217–#220 (unchanged; not rewritten here).

## 2. What P5.8 adds

* **`n8n-lego credentials` — the operator entrypoint that did not exist.** Before P5.8, key rotation, rotation recovery, backup and quarantine restore were library functions with no way for an operator to run them. `src/auth/credential-operator.mjs` (verbs) + a minimal `bin/n8n-lego.mjs` wiring. No new cryptography, no second vault. Mutating verbs refuse while the server's address:port is bound; nothing ever mints a key over sealed data; output never contains material. Declared scale-out exception (S3, accepted) for the operator-named backup file.
* **Cross-slice certification suite** `apps/n8n-lego/test/lego-p5-certification.test.mjs` — 51 tests, mostly against the real server as a separate process with file storage and **debug** logging.
* **Benchmark tool** `apps/n8n-lego/tools/p5/p5-certification-benchmark.mjs` (baseline vs P5, reproducible).
* **Operator runbook** `docs/n8n-lego/P5-SECURITY-OPERATOR-RUNBOOK.md`.
* No new contract row: the lock stays at **100 rows**; P5.8 closes the lock by asserting it (§3).

## 3. Certification matrix (tested — `lego-p5-certification.test.mjs`, 51 tests: 8 + 7 + 15 + 7 + 2 + 4 + 8)

| # | area | what is proven | result |
|---|---|---|---|
| 1 | contract-lock closure | all six P5 rows (`auth.principal`, `auth.session`, `auth.authorization`, `auth.credential-crypto`, `auth.account-security`, `auth.machine-identity`) present once, `stable`, owner agent-1, tests exist, **pinned exports == actual module exports**; lock = 100 rows; security barrel = 27 exports | 8/8 |
| 2 | one permission vocabulary | for **every global role × every canonical permission**, the REST role→scope source and the P5.3 engine agree (0 disagreements); nothing REST grants is outside the canonical universe; an unknown scope fails closed as UNKNOWN_PERMISSION even for the owner | 7/7 |
| 3 | end-to-end negative matrix | unauthenticated → 401 on 6 surfaces; 3 forged cookies → 401; API key as header / cookie / bearer on `/rest` → 401; cross-site Origin (even with a valid token), cross-site Referer, and no Origin/Referer → 403 and nothing changes; API-key scope escalation → 400; forgot-password byte-identical for real vs unknown account; password change kills other sessions; logout revokes server-side (replayed cookie → 401); login brute force → 429 within the per-account budget; malformed body carrying a secret → 4xx without echo (plus the setup run that feeds the scan) | 11/11 |
| 4 | leakage scan | canaries (password, new password, credential secret, raw API key) searched in **every** REST response body and header of the run (≥ 40 responses), the execution records of a workflow that references the credential, the complete **debug-level** server log, and **every file** in the data directory. Only allowed occurrence: the raw key in its one-time create response. Audit lines present and carry no `digest`/`material`/`rawApiKey` field | 4/4, 0 hits |
| 5 | confused deputy | a SecretRef for req-1/httpRequest/cap.http/v1 is refused for another request, audience, capability, or credential version; single use; an API-key principal without `credential:read` cannot mint; a delegated agent keeps the accountable identity, cannot mint beyond its grant, cannot escalate | 7/7 |
| 6 | stale authority | a cached ALLOW is reported `stale` and not served after a principal-version bump; an API key shrinks on the very next request after its owner is demoted | 2/2 |
| 7 | tenant boundary | authorization, SecurityContext binding, SecretRef mint, and delegation all refuse a cross-tenant request | 4/4 |
| 8 | operator drills (real data dir, real server) | mutating verbs refused while running; status/verify without material; backup 0600 + never overwrites + contains no secret; tampered backup rejected and nothing written; **interrupted rotation** (1 of 3 records moved) readable in the mixed state and finished by a plain server restart; **the pre-rotation backup is refused after rotation** (`backup-key-unavailable`, crypto-shredding); full rotation → fresh backup → wipe → restore → server re-seals every record (PATCH 200); **missing keyring** → CLI refuses and mints nothing, server serves metadata, answers 503 on secret operations and mints nothing; restoring the keyring file restores everything; memory storage and unknown verbs refused | 8/8 |

**Mutation check (measured):** each deliberate break below was reverted via git after the run.

| break introduced | detected by |
|---|---|
| operator ignores the server-running probe | §8 hook fails (`rotate must refuse while running`), exit 1 |
| operator proceeds without a keyring | 1 failing test |
| REST grants a scope outside the canonical universe | 4 failing tests |
| password change keeps other sessions alive | 1 failing test |

The first mutation also exposed a hygiene defect in the suite itself (a failed hook left the spawned server running and the run hung); fixed so every spawned server is killed on any failure.

## 4. Benchmarks (measured)

Machine: node v20.20.2, x64, **2 vCPU** Intel Xeon @ 2.60 GHz, **1985 MiB RAM** (low-resource VPS class). Reproduce: `node --expose-gc apps/n8n-lego/tools/p5/p5-certification-benchmark.mjs micro` and `node apps/n8n-lego/tools/p5/p5-certification-benchmark.mjs http --baseline <checkout of cc640344>/apps/n8n-lego --rounds 2`.

### 4.1 P5 primitives (in-process; P5-only — the baseline has no equivalent, so no comparison is claimed)

| operation | p50 ns | p95 ns | p99 ns | mean ns | heap B/op |
|---|---:|---:|---:|---:|---:|
| session validate (hit) | 153 | 260 | 335 | 178 | 45.7 |
| session validate (unknown id) | 152 | 258 | 434 | 183 | 46.7 |
| API-key authenticate (50 keys on owner) | 15783 | 32772 | 62752 | 19732 | 61.5 |
| SecurityContext construction | 2024 | 3383 | 5617 | 2658 | 447.8 |
| authorize ALLOW (uncached) | 384 | 434 | 547 | 414 | 167.8 |
| authorize DENY (cross-tenant) | 440 | 698 | 1942 | 631 | 182.6 |
| authorize DENY (unknown permission) | 442 | 670 | 1893 | 627 | 185.9 |
| authorizeCached warm hit | 438 | 655 | 898 | 495 | 213.2 |
| authorizeCached miss (distinct resources) | 2180 | 3199 | 10994 | 3298 | 484.2 |
| SecretRef issuance (mint + redeem, pair) | 5974 | 8677 | 25518 | 8046 | 735.4 |
| broker release (redeem only) | 997 | 1730 | 2445 | 2098 | n/a (batched) |
| SecretRef issuance (mint only) | 5459 | 9894 | 28354 | 8251 | n/a (batched) |
| vault open (AES-256-GCM, AAD-bound) | 10396 | 18235 | 42428 | 12971 | 149.3 |

Heap B/op is the heap delta across 30k operations without an intervening GC (an allocation-pressure indicator, not an exact allocation count). "n/a (batched)" rows are timed per operation inside batches that respect the broker's live-reference cap.

### 4.2 Decision cache hit/miss (bounded cap 4,096)

| workload | requests | hits | misses | hit ratio | entries / cap |
|---|---:|---:|---:|---:|---:|
| small working set: 4 principals x 10 actions x 50 resources (2,000 keys < cap) | 200000 | 198007 | 1993 | 99.0% | 1993 / 4096 |
| large working set: 64 principals x 40 actions x 500 resources, 80% on 13 principals x 100 resources (hot set ~52k keys > cap) | 200000 | 10262 | 189738 | 5.1% | 4074 / 4096 |

### 4.3 Bounded structures filled past their cap

| structure | inserted | retained | cap | heap at cap |
|---|---:|---:|---:|---:|
| sessions | 50000 | 10000 | 10000 | 3.26 MiB |
| decisionCache | 100000 | 4064 | 4096 | 1.58 MiB |

### 4.4 Baseline (pre-P5) vs P5 over HTTP — real server processes, same harness, interleaved rounds

Latencies in µs as p50 / p95 / p99. Login: 40 real scrypt logins over 10 accounts (under every limiter). Authenticated/unauthenticated GET: 2,000 each. Credential create: 200. Credential read: 1,000. Concurrency: 64 clients × 50 authenticated requests. RSS/HWM read from `/proc/<pid>/status` of the server process.

| round | build | idle RSS MiB | login (scrypt) | authenticated GET | unauthenticated GET (401) | credential create | credential read | 64-way rps | 64-way latency | RSS / HWM under load MiB |
|---:|---|---:|---|---|---|---|---|---:|---|---|
| 1 | baseline | 63.5 | 44357 / 51140 / 51758 | 621 / 1012 / 4238 | 416 / 633 / 2821 | 953 / 1402 / 3431 | 411 / 633 / 2586 | 3300 | 17154 / 33322 / 51826 | 96.2 / 96.2 |
| 1 | p5 | 64.6 | 39403 / 46068 / 50106 | 457 / 690 / 2481 | 355 / 480 / 2069 | 1127 / 1409 / 2901 | 397 / 589 / 2281 | 3419 | 16357 / 29279 / 64941 | 103.5 / 103.5 |
| 2 | p5 | 64.4 | 38921 / 48928 / 55575 | 442 / 667 / 2596 | 365 / 530 / 2332 | 1163 / 1833 / 3873 | 428 / 670 / 2618 | 3265 | 17102 / 37551 / 59929 | 105.5 / 105.5 |
| 2 | baseline | 62.3 | 39780 / 51297 / 55718 | 416 / 599 / 2059 | 374 / 560 / 2415 | 1171 / 2073 / 6958 | 384 / 555 / 2331 | 2849 | 19614 / 35086 / 64413 | 96 / 96 |

**Reading (measured, 2 rounds):** login, authenticated request, the 401 path, credential read and 64-way throughput overlap between builds; the round-to-round spread inside one build (baseline authenticated p50 621 vs 416 µs) exceeds the baseline-vs-P5 difference — **no measurable latency regression, and no improvement is claimed.** Credential create overlaps (P5 1,127–1,163 vs baseline 953–1,171 µs p50). The one consistent delta is **memory: +7.3 to +9.5 MiB RSS under load, ≈ +1–2 MiB idle** (P5 security state: session/limiter tables, keyring, decision structures).

## 5. Findings recorded by certification (not hidden)

1. **The P5.3 decision cache is not on the REST request path, and on this machine it does not pay for itself on the cheap path**: a warm hit (438 ns p50) is not faster than uncached `authorize` ALLOW (384 ns p50). REST enforces scopes through `compat/scopes` from the same canonical `roles.json`; §3 row 2 proves the two paths never disagree. The engine is used by SecretRef and machine identity. *Debt:* route REST authorization through the engine only when a need (per-resource or tenant policy) makes it worthwhile; drop or keep the cache on measured evidence.
2. **SecretRef is not yet used by execution** (carried from P5.4): the engine never touches credentials, so no secret can reach an execution record (§3 row 4 measured this), but nodes cannot yet consume credentials through SecretRef either.
3. **CSRF double-submit token is opt-in** (P5.2 contract, forced by the pinned upstream editor, which never sends it). Always-on defence: Origin/Referer validation + `SameSite=Lax`. §3 row 3 pins both halves.
4. **A finished rotation makes older credential backups unrestorable** (by design: crypto-shredding). Now surfaced by the CLI warning, the runbook, and a test.
5. **No supported password recovery without a mail transport**, and no admin-initiated reset (user management unsupported).

## 6. Test and gate results (measured on this branch)

| suite | result |
|---|---|
| backend `apps/n8n-lego/test/*.test.mjs` | **2375 / 2375** (P5.7 main: 2324; +51 certification) |
| frontend `packages/frontend-lego/test/*.test.mjs` | 418 pass, 0 fail, 1 skip (pre-existing) |
| gates | 7/7: ai:check, arch, arch:selftest, capabilities, scaleout, foundation, foundation:selftest |
| contract lock | 100 rows (unchanged by P5.8) |

Environmental note (not a regression): 5 backend tests fail when `N8N_LEGO_CATALOG_DIR` points at a directory whose `nodes.json`/`credentials.json` sit one level deeper (as `fetch-n8n-catalog.mjs --dir <x>/catalog` writes them); with the files at the catalog root they pass. CI builds the catalog correctly.

## 7. Remaining technical debt of the P5 plane

* P5.4: SecretRef not wired into execution; tenant is always `default`; credentials created before P5.4 have no `ownerId`.
* P5.5: per-host local keyring (declared scale-out exception, blocking once storage is shared — P8/P11); rotation work-list is an O(n) filter; no external KMS.
* P5.6: no mail transport; limiters, reset tokens and sessions are process-local; IP limiter keys on `remoteAddress` only; MFA enforcement deferred.
* P5.7: no `/api/v1` mount (the authenticator exists, nothing calls it); no REST/UI and no ownership transfer for service principals; key revocation deletes instead of tombstoning; members (not only admins) may create service principals within their own grant.
* P5.8: decision cache unused on REST (finding 1); operator CLI is single-host and offline-only.

## 8. Rollback

`git revert -m 1 <P5.8 merge>` removes only the CLI verb, tests, tool and docs; no data format changed. Full P5 rollback order and the P5.5 downgrade warning: runbook §13.
