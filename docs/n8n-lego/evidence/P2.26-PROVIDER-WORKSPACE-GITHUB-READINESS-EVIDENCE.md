# P2.26 — Provider, Workspace & GitHub Integration + Foundation Readiness — Evidence

- **Milestone:** P2.26 · owner agent-1 · completed 2026-09-23.
- **Protected main (working baseline):** `d15a9538b1899e9fd2ea9d5bd37d3fd26ecc1943`
  (the P2.25 completion-transition merge).
- **Scope (Master Prompt P2.26):** provider declaration profile (`sim/local/cloud` —
  declared, never assumed), workspace integration boundary preserved, GitHub
  integration boundary (handed-over / scoped / request-bound, never a stored
  credential), foundation-readiness re-verification of every P2.16–P2.25
  invariant — **plus the P2.27 architecture reservation only** (design document,
  planned register row, roadmap planning section; **zero P2.27 implementation**).

## §1 Baseline

Session sandbox reset before P2.26 began: every local copy under `/home/user` was
git-less. Repository **re-cloned** from origin; HEAD verified `d15a9538` (= PR #72
transition merge), working tree dirty **0**, open PRs `[]`. Chain re-verified from
remote history: P2.25 impl `9cc6ba88 · PR #71`, transition `d15a9538 · PR #72`.
**Freeze baseline = `d15a9538`**; pre-change baseline run: gates 7/7, BE
866/3 (rest pre-existing), FE 418/0/1 — no drift introduced by the re-clone.
Git author identity restored to `Catzpro01 <133132598+…@users.noreply.github.com>`.
Askpass helper recreated from the standing token file (contents never printed).

## §2 Baseline correction (§0)

P2.26 was born with `startEvidence.commit = 9cc6ba88` (P2.25 *implementation*
merge). Corrected during the initial governance pass (`e12fd970`):

- `startEvidence.commit = d15a9538…` — the actual protected main from which
  P2.26 starts;
- the role string now **documents the distinction** (`9cc6ba88 stays P2.25
  implementation evidence`) instead of hiding it;
- P2.25 `finishEvidence` (`9cc6ba88`, PR 71) and every historical row untouched
  — no history rewritten (asserted by the new suite);
- top-level `mainBaseline` stays `9cc6ba88…` until the P2.26 completion
  transition (established convention — only `startEvidence` was in §0 scope).

## §3 P2.27 architecture reservation (recorded BEFORE implementation)

Per §§34–38, in the governance commit (`e12fd970`) — reservation only:

- **`docs/n8n-lego/P2.27-PLUGIN-RUNTIME-DESIGN.md`** — planning/design document,
  **no implementation code** (test asserts no js/ts code fences): Core
  responsibilities, plugin boundary, manifest, capability security, trust classes,
  runtime classes + locality, language independence, secret broker, tenant
  context, resource budgets, supervisor/quarantine, circuit breaker, deadline,
  contract replay, upgrade/rollback, supply chain, frontend plugin boundary,
  performance locality, scaling model, P2 non-regression rule — the §38
  acceptance checklist is embedded and marked, and the banner states
  **NO P2.27 IMPLEMENTATION IS STARTED**.
- **Register row `P2.27`** — status `planned` (future/planned reservation),
  phase F, owner manager, dependencies = the exact §37 list (all resolve to real
  rows), `purpose`, `deliverables`, `implementationBoundary` containing the
  verbatim sentence `NO P2.27 IMPLEMENTATION IS STARTED BY THIS PROMPT.`, full
  `nonScope` (§7 + §31), `testsGates`, `decisionDependencies = []`, §38
  `completionCriteria`, `startEvidence = d15a9538` with the reservation role.
  **`currentMilestone` NOT moved** (stays P2.26); the chain is NOT rewired
  (P2.26 → `P2.17+` until the completion transition — asserted).
- **Roadmap** `P2.17-P2.26-ROADMAP.md` gained a clearly marked post-P2.26 section
  `## P2.27 — Contract-Driven Pluggable Runtime & Security Foundation` labelled
  **PLANNING-ONLY · FUTURE IMPLEMENTATION · MANAGER-AUTHORIZED RESERVATION**,
  with principle, reserved scope, §37 dependencies, not-this-milestone list and
  Exit = "dedicated Master Prompt first". Completed P2 history untouched.
- `.ai` regenerated in the same commit (`lego:ai` before `lego:ai:check`).

## §4 Provider declaration profile (contract `ai.provider-declaration@1.0.0`)

**Lock row 32** — owner `manager`, domain `ai-foundation`, status `implemented`,
surface `src/lego/provider-declaration.mjs`, 9 exports sorted ASCII, tests
`lego-provider-declaration.test.mjs`, no operations/permissions (a declaration
profile is not a capability — **capability list stays exactly 17**, FE pin
`aiFoundationCapability===17` untouched, conformance count unchanged).

`apps/n8n-lego/src/lego/provider-declaration.mjs` (zero `node:*`, zero
`console.*`, no clock/identity minting; imports = `ai-foundation.mjs` +
`token-usage.mjs` only — asserted):

- `PROVIDER_PROFILES = [sim, local, cloud]` quoted byte-for-byte from
  `manifest#providerProfiles.profiles`; `describeProviderProfile` answers `null`
  for anything else — **no case folding, no fourth profile, no fallback**;
- `assertProviderDeclaration` — closed three-field shape
  (`providerId, kind, profile`); every missing field refuses with
  *"declared, never assumed, discovered or selected by default"* — **nothing is
  defaulted**, so there is no auto-selection path; kind must be a published
  `PROVIDER_KINDS` word; profile must be exact; unknown fields refuse
  (configuration stays on the P2.25 adapters); forbidden credential field names
  refuse by name without echoing values; `providerId` is an opaque bounded
  reference refused for full-form credentials, paths, traversal and overlong
  values (canonical detector `SENSITIVE_USAGE_RE` quoted from
  `ai.token-usage@1.0.0`); result is a frozen copy (input mutation later cannot
  rewrite what was declared);
- manifest `providerProfiles.rule` states declared-not-assumed with every hard
  prohibition (discovery, selection, standing authority, stored credential,
  explicit configuration); per-profile `configRule` all require explicit
  declarations; `sim` never becomes a fallback; `cloud` authority never stores
  a credential;
- **zero-install holds**: no declaration = honest `not-configured`, valid state
  (asserted), provider kinds/transport lists unchanged.

## §5 Workspace boundary

Preserved, not extended: `ai.workspace@1.0.0` row untouched (version, four
operations, two permissions, export byte-parity with `workspace.mjs`); the
Workspace keeps owning `src/lego/workspace.mjs` + manifest — **ai-foundation
paths never absorb them** (paths grew only by the new declaration module,
19 total); **exactly one `createWorkspaceManager` implementation exists** in
`src/lego` (second-implementation guard); static authority scan: the Workspace
module grows no `child_process`/HTTP/net import, and the new module introduces
no filesystem/shell/subprocess/network authority; manifest `workspaceBoundary`
records the rule (internals never move to Foundation, no second implementation,
no unrestricted filesystem/shell/subprocess authority) with the never-list.

## §6 GitHub boundary

GitHub remains an **integration boundary, not a Core capability** — all canonical
declarations preserved and mechanically enforced:

- `applicationProvider.firstClassRule` still forces native ∧ gateway as two
  bindings of ONE capability set (asserted /native/i + /gateway/i);
- manifest `applicationProvider.handedOverAccess` (new, quoted byte-identically
  into `INTEGRATION_ACCESS_RULES`): rule = **explicitly handed over, scoped,
  request-bound**; `never = [stored credential, standing GitHub token, global
  repository authority, implicit account discovery]`; replaceable + "not a
  built-in dependency of every plugin"; `decides = ai.approval` — the handoff
  proves well-formedness only, **configuration is never authority**;
- `assertHandedOverAccess` enforces it: closed shape (a `repository` field
  refuses — global repository authority is not a field), opaque `grantReference`
  + declared capability-word `scope` (non-empty, ≤32, word grammar) +
  `requestId` (**request-bound — without it access is "never standing"**);
  raw credentials refuse by full canonical form while **bare prefixes do not
  false-positive** (P2.23 detector lesson, asserted `sk-x`/`ghp_` pass);
  credential-shaped scope words refuse as `credential-shaped-value`;
- `lego-set#externalActionModel`: `github.create_pr` stays
  `sideEffects: external`, `approval: required`, `workspaceScoped: false`,
  status `contract-only — none … is implemented`; `git.*` stay workspace-scoped;
- **replaceable in practice**: no `github.com` string and no HTTPS client import
  exists anywhere under `src/lego` (directory-wide scan); no GitHub SDK, no
  standing token, no account discovery code exists to remove later.

## §7 Readiness re-verification (proof, not redesign)

New focused suite `apps/n8n-lego/test/lego-provider-declaration.test.mjs` —
**29/29** — organized as the readiness proof: contract/lock byte-parity (row 32,
neighbouring contracts untouched, capabilities stay 17, ownership split) ·
profile vocabulary + declaration rules · declaration validator matrix ·
handed-over-access matrix · workspace boundary (row/exports/single
implementation/authority scans) · **register walk: P2.16–P2.25 all `complete`
with start+finish evidence intact** (P2.24 `a409d930`, P2.25 `9cc6ba88`/PR71,
P2.23 start `ab4a5898` exact per standing corrections) · pointers
(`current=P2.26`, `previous=P2.25`, §0 correction landed) · P2.27 still
`planned` with zero implementation artifacts (forbidden module names absent,
design doc planning-only) · honesty list (notImplemented 6→8, required claims
preserved) · one error family with `lego.contract_violation` published in
errors contract **1.2.0 (untouched)** and detail redaction · ai-set mirror left
historical (canonical pointer = `milestones.json`).

## §8 Full gates

`lego:arch` · `lego:arch:selftest` · `lego:foundation` · `lego:foundation:selftest`
· `lego:capabilities` · `lego:scaleout` · `lego:ai:check` — **7/7 PASS**
(arch clean with **32 locked contracts**; foundation F16 clean — the new module's
quoted codes all publish; capability conformance **23 REST features vs 91
registered capabilities** (unchanged — no capability added); scale-out no new
finding; `.ai` in sync **64/37/101**, produced by root `node tools/lego/ai-pack.mjs`
before `--check`).

## §9 Backend / frontend validation

- **Backend full suite:** **898 tests · 895 pass · 3 fail** — the three fails are
  the pre-existing `rest.test` 404s (§13). New work contributes **29/29**.
- **Frontend full suite:** **419 tests · 418 pass · 0 fail · 1 skip** (boot pin
  `18_126` green; register pins FE32/FE33 green with the §0 correction; FE34
  lock-count pin 31→32 updated with the P2.26 message).
- **P2.16–P2.25 regression slices (+ ai-set, ai-foundation, both adapter suites,
  P2.26): 17 files — fail 0.**

## §10 Failure classification (ABORT-IF-RED review)

1. **`lego-provider-adapters` count-pin `31`** — after lock row 32 the P2.25
   suite's own row-count pin went red (the file did not exist when the original
   five pins were listed). Fixed **at the root**: per-context pin 31→32 with a
   message naming P2.26's thirty-second row — the same count-pin discipline every
   prior milestone applied; no test deleted, no gate weakened. Re-run: 38/38.
2. **`rest.test` ×3 404s** — PRE-EXISTING BASELINE (present since the P2.22
   baseline `770/767/3`, unchanged through P2.25). FOLLOW-UP, not touched.
3. Test-authoring defects while writing the new suite (workspace-domain owner
   assertion, manifest key name, empty-string kind/profile hitting the required
   check first, a Cyrillic typo inside one regex, `finishEvidence.pr` assumed on
   pre-PR-era rows) — each fixed in the **test**, at the root; module behaviour
   never bent to satisfy a test.

## §11 Main verification (local, pre-merge)

On branch tree = content of §4–§7: gates **7/7** · BE **898/895/3** (rest
pre-existing) · FE **419/418/0** · regression **fail 0** · XA-21 boot byte-pin
green inside the FE suite · XA-21 evidence JSONs (`frontend-memory-p214.json`,
`frontend-context-session-p213.json`) **0 diffs** · register consistent (FE32/33)
· `.ai` fresh (`lego:ai:check` OK). PR/merge SHAs are recorded in the §12
completion-transition update below.

## §12 PR / merge evidence

- implementation PR: branch `feat/agent-1-p2.26-foundation-readiness`
  (`e12fd970` governance/reservation + implementation + evidence commits) →
  pushed, remote-validated (GET pull → mergeable) → merged with
  `merge_method: merge` — SHA recorded in the completion-transition commit;
- transition PR: register/evidence completion update, merged after §11 stayed
  green post-merge (its number is the PR carrying commit
  `P2.26 completion transition + advance to P2.27`).

## §13 Register transition

Performed in the completion-transition commit after the implementation merge:

- P2.26 `status = complete`, `finishEvidence = {protectedMain, pr, mergeCommit,
  role: post-merge verified protected-main completion}`, reconciliation marked
  `historical` with a resolution note, `nextMilestone` corrected to exact
  `P2.27`, `verificationEvidence.local` = §11 numbers, official PENDING;
- `currentMilestone = P2.27`, `previousCompletedMilestone = P2.26`,
  `mainBaseline` = implementation merge SHA;
- **P2.27 row becomes the current administrative milestone** using the
  established convention (status `in-progress`, chain P2.26 → P2.27 → `P2.17+`)
  while **P2.27 implementation stays ZERO** until its Master Prompt — FE chain
  and register pins updated in the same commit; historical rows untouched.

## §14 Known pre-existing failures

- `apps/n8n-lego/test/rest.test.mjs` — 3×404 (`GET /rest/types/nodes.json`,
  `GET /rest/types/node-versions.json`, `POST /rest/node-types`) — unchanged
  since P2.22; **PRE-EXISTING BASELINE**, FOLLOW-UP.
- Scale-out findings S3/S4 (`store.mjs` disk system-of-record, `bin`
  `process.env`) — pre-existing, owned, declared exceptions; none added.
- `npm run lego:test` quoted-glob quirk (known since P2.23): the seven official
  gates are §8; suites run with unquoted globs; `package.json` out of scope.
- `ai-lego-set.json` mirror fields (`currentMilestone: P2.16` etc.) — historical
  planning material predating the canonical register; `ai-pack` prefers
  `milestones.json`; deliberately NOT rewritten (asserted as historical).

## §15 Official validation status

**PENDING — truthfully.** Official validation remains the GitHub Actions
self-hosted Windows runner on this PR. Local full validation is PASS (debug
evidence, not official sign-off). No official `PASS` claimed; P2.23–P2.25
`official: PENDING` not rewritten.

## §16 XA-21 / boot payload

XA-21 holds through the standing mechanism: the **18,126 B boot descriptor is
byte-pinned inside the frontend suite** — FE `418/418 pass` is the §16
verification (any boot-pin flip would be red). The XA-21 evidence trail files
are untouched (**0 diffs**). Manifest `memory.json#XA-21` descriptor-assembly
claim unchanged. P2.26 added no boot-path file: the new module lives in the
backend; FE sources changed only the FE34 count-pin message (test-only).

## §17 Non-scope held (P2.27 zero, P3/FUTURE intact)

No Plugin Manager/Supervisor/sandbox/broker/runtime/WASM runtime/Python worker
manager/hot-swap/rollback engine exists (forbidden module names asserted
absent); no microservice/K8s/marketplace/rewrite work; `contracts/errors.contract
.json` stays 1.2.0; `.ai/capabilities.json` untouched (boot 18_126); XA-21
untouched; `tools/gateway/*`, `.arena/registry/agents.yaml`, `crates/**`
untouched; GitHub/workspace authority lists unchanged; provider credential
discipline (P2.25) extended by declaration+handoff validators, never relaxed.

## §18 History & chain integrity

Re-clone verified the full remote chain before any edit (no local history was
forged); governance commit `e12fd970` precedes implementation per the §1 order;
P2.22/P2.23/P2.24/P2.25 chain SHAs and evidence anchors verified untouched
(`3478e8cc`/`389c6b5d`, `ab4a5898`/`82f4d305`, `a409d930`/`f28e4fc3`,
`9cc6ba88`/`d15a9538`); blanket SHA replacement never used — every pin moved
per-context with an explicit message; edit → validate GREEN → commit order held
throughout; no force-push, no rewrite, no branch deletion, no test removed for
green, no fabricated evidence; `currentMilestone` stayed `P2.26` for the whole
implementation phase (asserted), and only the completion transition may advance
it — P2.27 implementation waits for its Master Prompt.
