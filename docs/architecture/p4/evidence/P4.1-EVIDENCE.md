# P4.1 Evidence — Canonical Ingress Contracts

**Slice:** P4.1 | **Branch:** `arena/p4.1-ingress-contracts` | **Base:** `main@75189576`
**Owner:** Arena Agent 2 (P4 Marathon) | **Tanggal:** 2026-09-23
**Scope:** beku `ingress.contracts@0.1.0` — value contracts P4 (tanpa runtime).

## Gate A — Repository integrity

| Check | Hasil |
| :--- | :--- |
| Base branch | `main@75189576` (HEAD terbaru saat branch dibuat; anti-staleness §5 ditegakkan — branch pertama di '@454ef1d3 dibuang karena main bergerak 4 commit) |
| File diubah | `crates/n8n-common/src/ingress_contract.rs` (BARU), `crates/n8n-common/src/lib.rs` (+1 mod, +1 re-export block) |
| File **tidak** diubah | `Cargo.toml` root, `Cargo.lock`, `contracts/**`, `.arena/policies/**`, file agent lain (`apps/n8n-lego/**`, dokumentasi P3, `crates` lain) |
| Kebijakan path | `.arena/policies/paths.yaml`: `contracts/**` + `Cargo.lock` = global read-only → dipatuhi; kontrak P4 hidup di `docs/architecture/p4/` + kode modul |
| Force push / direct push main | Tidak ada |

Alasan penempatan di `n8n-common`: modul kontrak (precedent `expression_contract.rs`),
tanpa tambahan dependency apa pun → `Cargo.lock` tidak berubah; downstream
(`n8n-workflow` dsb.) dapat mengonsumsi tanpa siklus dependency.

## Gate B — Build

Toolchain sandbox: `tools/rust-offline-rig` (Rust 1.88.0, vendor 27 crates dari git tag persis `Cargo.lock`).

```
RUST_RIG=/var/tmp/rust-rig bash tools/rust-offline-rig/run.sh test
→ cargo check/test --workspace: BUILD OK, 0 warning.
```

Catatan rig (untuk sesi berikutnya, juga tercatat di progress file):
`/tmp` adalah tmpfs 1 GiB → rig dipindah ke `RUST_RIG=/var/tmp/rust-rig`;
vendor sempat korup (file 0-byte dari clone saat disk penuh) → diperbaiki dengan
hapus clone `vendorsrc/regex-syntax-0.8.11` + `vendor/` + shared `target/`, lalu re-run setup.

## Gate C — Focused tests (baru, 30 tes, semua hijau)

`n8n_common::ingress_contract::tests` — 30/30 OK:

* wire fidelity verbatim n8n: execution modes (10), activation modes (6), route prefixes (6), HTTP methods (6), webhook auth (4), response modes (4), response data (4) — string persis hasil grep dari `reference/n8n`;
* admission states (8) persis kosakata Issue #99 §D;
* boundedness: payload inline cap 64 KiB (terukur serialisasi), id ≤128, header/query caps, reason code ≤64;
* fail-closed: envelope versi asing, deadline ≤ received, sumber HTTP tanpa method/path, external tanpa keputusan security, `event` tanpa keputusan security, header non-lowercase, sha256 non-hex, `duplicate` tanpa `duplicateOf`, `responseData` di luar `lastNode`, status code di luar 100–599;
* generation fencing: exact-match saja (lebih tua & lebih baru sama-sama ditolak);
* activation: 16 transisi kanonik diterima, 14 contoh ilegal ditolak, tanpa self-loop, happy-path lifecycle penuh INACTIVE→…→INACTIVE, invariant record (`drainDeadlineMs`, `lastError`);
* hand-off: `ExecutionRequest::from_envelope` membawa seluruh identitas + fence; menolak workflow/generation unresolved dan envelope kedaluwarsa;
* end-to-end mini: envelope gen 7 vs route gen 8 → `StaleGeneration{7,8}`.

## Gate D — Regression

Pre-existing suite tidak tersentuh dan tetap hijau pada tree yang sama:

| Suite | Hasil |
| :--- | :--- |
| n8n-connection | 2/2 |
| n8n-execution-data | 20/20 |
| n8n-expression | 37/37 |
| n8n-node-model | 1/1 |
| n8n-nodes-rust | 2/2 |
| n8n-validation | 4/4 |
| n8n-workflow (unit) | 70/70 |
| n8n-workflow conformance | 2/2 |
| graph_expression_integration | 1/1 |
| reference_fixtures | 5/5 |
| trigger_lifecycle | 4/4 |
| runtime_runner_test | 3/3 |

Total workspace pasca-P4.1: **181 tests, 0 failed, 0 ignored.**

## Gate E — Compatibility

Tidak ada perubahan perilaku apa pun pada kode existing (modul baru, additive).
Kosakata wire diverifikasi verbatim terhadap sumber ter-pin (tabel di
`docs/architecture/p4/ingress-contracts.md` §5). Tidak ada perubahan workflow JSON,
tidak ada kontrak node, tidak ada perubahan `contracts/**`.

## Gate F — Race/Failure (level kontrak)

| Skenario | Mekanisme kontrak | Test |
| :--- | :--- | :--- |
| Stale generation | `fence_generation` exact-match | `generation_fencing_*`, `stale_generation_block_*` |
| Replay dari "masa depan" | fencing menolak observed > current | sama |
| Double activation | `CANONICAL_TRANSITIONS` tanpa `Active→Activating`, tanpa self-loop | `illegal_activation_transitions_*` |
| Deadline lewat sebelum hand-off | `from_envelope` menolak expired | `execution_request_rejects_*` |
| Duplikat | `AdmissionDecision` Duplicate wajib membawa asal | `admission_decision_invariants_*` |
| Auth belum diputuskan untuk edge | `MissingSecurityDecision` + `must_admit` hanya `allow` | `envelope_rejects_external_*`, `security_decision_*` |
| Malformed/unresolved | validate() + hand-off guard | envelope/admission/payload suites |
| Shutdown/update race | state machine punya `Draining→Deactivating` + invariant drain | `happy_path_*`, `activation_record_invariants_*` |

(Race runtime sesungguhnya — journal, rekonsiliasi — ada di P4.7; kontraknya
disiapkan deterministic di sini.)

## Gate G — Resource

P4.1 tidak menambah hot path runtime (nilai + invariant murni). Boundedness
by construction: seluruh ukuran dicap oleh konstanta kontrak (lihat §3 tabel).
Tidak ada klaim performa — benchmark gate #109 menjadi bagian P4.3 (route lookup)
dan P4.5 (admission).

## Gate H — Documentation

Kontrak formal: `docs/architecture/p4/ingress-contracts.md` (`ingress.contracts@0.1.0`).
Dok ini: evidence. Progress/hand-off: `.arena/progress/agent-2-p4.1-ingress-contracts.md`.

## Gate I — PR evidence summary

* **Scope:** value contracts P4 beku + 30 tes (tidak ada runtime).
* **Files:** 2 kode + 2 dok + 1 progress.
* **Behavior change pada kode existing:** NOL.
* **Compatibility:** wire kosakata verbatim n8n 2.9.4; tidak ada JSON migration.
* **Known limitations:** lihat dokumen kontrak §9 (performance belum diklaim; tenant placeholder).
* **Rollback:** revert PR ini = hapus modul + re-export; tidak ada state lain yang berubah.
