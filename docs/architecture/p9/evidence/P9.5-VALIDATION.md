# P9.5 validation — 2026-09-24

Baseline main: `24032a0ccdb388c4e81b87f3e8c11220fb681feb` (P6.13 merge).
Preflight: P9.1–P9.4 verified merged on main; no P9.5 branch existed.
Node (sandbox), Linux x64, shared host.

## Coverage

13 dedicated tests: published vocabulary/owner/schema-version fixture match;
classifyEventVersion fail-closed matrix; full P3/P4/P6 lifecycle fixtures
(happy + failure) covering every published name with shared correlation;
canonical byte-identical round-trip with allowlist-order attribute
normalization; version-compatibility fixture (unknown contract/event/schema
versions all reject); max-occupancy size bound under wireBytes; attribute
redaction-without-read and oversize/wrong-type rejection; no payload/message/
unknown top-level fields; creation performs no encoding/clock/I/O (source
scan + traps); monotonic order gate (duplicate vs out-of_order vs capacity vs
rejected); unordered policy + invalid config fail-closed; frozen events over
branded context for all 20 names; schema document + locked row conformance
including count-pin 58.

Commands:
- `node --test apps/n8n-lego/test/lego-semantic-events.test.mjs` → **13 pass / 0 fail**
- `node --test apps/n8n-lego/test/lego-{telemetry-envelope,structured-log,metric-governor,trace-context,semantic-events}.test.mjs` → **61 pass / 0 fail**
- `npm run lego:ai` (regeneration after domains.json path/test/phase P9.5 + contract 1.4.0)
- full `npm run lego:gate` — see PR verification comment for exact final counts

Shared-surface exact delta (this slice only):
- `contract-lock.json` +1 row `observability.semantic-event@1.0.0` (57 → 58).
- Count-pins in 17 test files updated 57 → 58 with explicit P9.5 suffix; no
  pin deleted, no assertion removed, no prior row edited.
- `domains.json` observability: phase P9.4 → P9.5, contract 1.3.0 → 1.4.0,
  +path/+public `src/lego/semantic-events.mjs`, +test. **Owner stays `agent-6`.**
- `BACKEND_LEGO.md` +1 changelog row (additive new contract).
- `.ai/*` regenerated by ai-pack from the canonical manifest (not hand-edited).

No P3/P4/P6/P8 source import; mustNotDependOn list unchanged; no error-registry
modification; no new dependencies.

## Benchmark

Raw data: `p9.5-event-benchmark.json`; script `tools/p9/semantic-event-benchmark.mjs`.
Each mode: 10k warmup, then 100 batches of 1000, explicit GC before/after.

| Mode | elapsed ms | ops/s |
| --- | ---: | ---: |
| OFF arithmetic control | 2.261 | 44,232,717 |
| minimal lifecycle event | 337.656 | 296,159 |
| event with attributes + sequence | 382.349 | 261,541 |
| monotonic order gate accept | 4.909 | 20,369,063 |

Microbaseline only: creation admission on a shared sandbox. Not workflow
throughput, not allocation counts, no sink/queue/backend I/O benchmarked.
Batch-average percentiles are not individual-event p95/p99.

## Known limitations

No buffer, exporter, retention, sampling, causal graph or operator API
(those are P9.6+). redactionCount is admission-local and never on the wire.
Decode re-validates through the P9.1 context brand; tenant decode requires
explicit policy. Vocabulary is closed at 1.0.0 — new names need a contract
version decision.
