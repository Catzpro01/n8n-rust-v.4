# P9.2 validation — 2026-09-23

Baseline main: `1a7fc1aa2a0d6f5b1d54c4fcae46f6df5c52de30`.
Preflight: P9.1 post-merge full gate passed (1004 backend, 418 frontend + 1 skip).
Node 22.18.0, Linux x64, Xeon 2.60 GHz shared sandbox.

## Coverage

11 dedicated tests: structured fixture, all six severity levels, complete
source-registry taxonomy fixture, malformed error/code/class rejection, message
redaction before record creation, secret attribute removal without getter
execution, bounded attributes and malformed values, mandatory structured
identity, deterministic bounded serialization, no encoding/clock/I/O in creation,
schema and contract-lock conformance.

Commands:
- `node --test apps/n8n-lego/test/lego-structured-log.test.mjs`
- `N8N_LEGO_CATALOG_DIR=/home/user/.n8n-lego/catalog npm run lego:gate`
- `npm run lego:ai` (regeneration), `lego:ai:check` in aggregate gate
- `git diff --check`

Final local full gate: **1015 backend pass / 0 fail**, **418 frontend pass / 0 fail / 1 existing skip**. Dedicated suite: **11 pass / 0 fail**. No new dependencies,
no skipped new tests, no error-registry modification. Exact lock inventory
maintenance: 40 -> 41 for the one new P9.2 row, retaining equality and all
prior contract identity/owner/export assertions. The existing #130 baseline
Rust formatter issue is outside P9; GitHub check results must be classified
at PR verification, not inferred from this local evidence.

## Benchmark

Raw data: `p9.2-log-benchmark.json`; script `tools/p9/structured-log-benchmark.mjs`.
Each mode: 10k warmup, then 100 batches of 1000, with explicit GC before/after.

| Mode | elapsed ms | ops/s | batch-mean p50/p95/p99 us |
| --- | ---: | ---: | --- |
| OFF arithmetic control | 1.700 | 58,832,631 | 0.013 / 0.016 / 0.033 |
| normal log | 391.561 | 255,388 | 3.873 / 4.179 / 4.341 |
| classified error + redaction | 583.745 | 171,308 | 5.470 / 6.832 / 8.871 |

Normal log CPU 395480 user + 102 system us; pre-GC heap delta 103952 B;
after-GC retained heap delta 24504 B; RSS delta 270336 B. Error/redaction CPU
606154 user + 5 system us; heap delta 611024 B, retained delta 27336 B, RSS
516096 B. These are observations, not allocation counts or workflow overhead.
Batch-average percentiles are NOT individual event p95/p99. JIT/GC/shared-host
noise apply. No sink, retained queue, backend I/O or workflow is benchmarked.

Only P9.2 measured. No zero-allocation claim; full T0–T14 remains P9.22.
