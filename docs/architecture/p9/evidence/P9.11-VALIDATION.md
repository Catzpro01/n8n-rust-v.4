# P9.11 Validation Evidence — Execution Diagnostics Contract

Slice: P9.11 · Contract: `observability.execution-diagnostics@1.0.0` · Branch: `arena/agent4-p9.11-execution-diagnostics` (stacked on P9.10 `9c2d1d2e`)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-execution-diagnostics.test.mjs` — **9 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| Failed execution correlated to workflow/version/trigger/node/runtime | `failedDiagnostic` | all five identity fields asserted on diagnostic |
| Resource state attachable | `resource` + summary `hasResource` | numeric snapshot retained |
| Checkpoint/replay/artifact refs | `refs` + summary `hasRefs` | all three ref kinds asserted |
| Diagnostics bounded | `EXEC_DIAG_LIMITS` (wire 8192, refs 16, nodes 32, resource 16) | serialize enforces wire cap |
| Large state referenced, not duplicated | `largePayload` (8192 B+) | `state_ref` + `bytes`; raw payload absent from wire |
| Usable with missing evidence | `missingEvidenceDiagnostic` | valid diagnostic + summary; `evidenceMissing` lists 9 fields |
| Secret-safe proven | `secretSafe` | secret identity → null; secret message → marker; secret refs → null; serialize rejects secret shapes |
| Standardized failure summary | `summarizeFailure` + fingerprint stability | `FFP:v1:` stable across rebuilds, differs on code change |

## Full gates (`N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| backend full | **1497 pass / 0 fail** |
| dedicated execution-diagnostics | **9 pass / 0 fail** |
| `lego:arch` | OK — 26 domains / **64 contracts** |
| `lego:arch:selftest` | 26/26 |
| `lego:foundation` | OK |
| `lego:foundation:selftest` | 15/15 |
| `lego:capabilities` | OK |
| `lego:scaleout` | OK |
| `lego:ai:check` | OK — 64 gen / 37 curated / 101 total |
| frontend full | **418 pass / 0 fail / 1 skip** |

## Bugfixes during slice (do not regress)

1. **Required identity on all outcomes** — `executionId`/`workflowId` missing ⇒ `null` even for `success` (earlier: success allowed empty identity).
2. **`safeRef` auth-prefix floor** — P9.7 `containsSecretShape` Bearer pattern needs ≥8 token chars; refs also reject `/\b(bearer|basic)\s+\S/i` and assignment-style secrets so short `Bearer dG9rZQ` cannot enter checkpoint/replay/artifact ids.

## Classification decisions (not bugs)

- Success + `failure` block ⇒ `null` (contradictory spec).
- `failure.class` outside `EXEC_DIAG_FAILURE_CLASSES` ⇒ `null` (closed vocabulary).
- Poisoned `JSON.stringify` ⇒ `createExecutionDiagnostic` returns `null` (wire bound fail-closed), never throws.
