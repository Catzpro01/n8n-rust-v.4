# P9.20 Validation Evidence — Observability Contract Oracle + Cross-Domain Acceptance

Slice: P9.20 · Contract: `observability.contract-oracle@1.0.0` · Branch: `arena/agent4-p9.20-contract-oracle` (stacked on P9.19 `f76d9692` / PR #203)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-contract-oracle.test.mjs` — **10 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| P3 contracts validated | `matrices.P3` | 2 clean / 1 multi-drift |
| P4 contracts validated | `matrices.P4` | 1 clean / 2 drift |
| P6 contracts validated | `matrices.P6` | 1 clean / 2 drift (incl. major jump) |
| P8 contracts where available | `matrices.P8` | 1 clean / 1 drift |
| Missing-field detected | absent `workflowId` + type mismatch | `missing_field` |
| Invented-field detected | `totallyMadeUpField` | `invented_field` |
| Vocabulary drift detected | `status:'compleet'`, `method:'FETCH'`, `tier:'SSD'` | `vocabulary_drift` |
| Version incompatibility | `0.9.0`/`2.0.0`/`1.0.0@^0.1.0`/missing | `version_incompatible` |
| Deterministic output | dual fresh oracles | `deepEqual` reports + stable fingerprint |
| Cross-domain fixtures checked in | `fixtures/p9/contract-oracle.json` | schemas + 4 matrices |

## Required evidence fixtures

| Case | Fixture path | Result |
|---|---|---|
| P3 matrix | `matrices.P3` | validated |
| P4 matrix | `matrices.P4` | validated |
| P6 matrix | `matrices.P6` | validated |
| P8 matrix | `matrices.P8` | validated (available) |
| Intentional drift | P3 row 3 | all four codes fire |
| Missing field | dedicated test | `workflowId` flagged |
| Invented field | dedicated test | `totallyMadeUpField` flagged |

## Full gates (`N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| dedicated contract-oracle | **10 pass / 0 fail** |
| backend full | **1591 pass / 0 fail** |
| frontend full | **418 pass / 0 fail (+1 skip)** |
| `lego:arch` | **OK — 26 domains, 73 contracts** |
| `lego:arch:selftest` | **26/26** |
| `lego:foundation` | **OK — 26 LEGOs, 12 node creation routes** |
| `lego:foundation:selftest` | **15/15** |
| `lego:capabilities` | **OK — 23 REST features vs 91 registered capabilities** |
| `lego:scaleout` | **OK — all findings declared/owned** |
| `lego:ai:check` | **OK — Generated 64 / curated 37 / total 101; Locked contracts 73** |

## Fixes during slice (do not regress)

1. **Version keys exempt from invented_field** — `contractVersion`/`version` are schema meta, not specimen inventions.
2. **Type mismatch → `missing_field` with `value:'type_mismatch'`** — keeps finding codes fixed while still surfacing wrong types.
3. **Sort key is (code, field, value)** — string compare, locale-independent; fingerprints join on `:`.
4. **Local `satisfies`** — same range semantics as lego-foundation public compat model; R4 boundary forbids importing private `src/lego/compat.mjs`.
5. **Contract-lock 72→73** — clean append; count-pins 72→73.
6. **Arch/F16** — sole import telemetry-redaction; no bare unpublished namespace strings.

## Classification decisions (not bugs)

- Missing version field is `version_incompatible` (not a separate code) — four-code vocabulary is the contract.
- `expectCodes` compared as sets (sorted unique) — a specimen may trip the same code on multiple fields.
- P8 is "where available": fixture ships a real matrix; absence would be noted, not failed.
