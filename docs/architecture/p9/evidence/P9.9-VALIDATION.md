# P9.9 Validation Evidence — Resource / Pressure Telemetry

Slice: P9.9 · Contract: `observability.resource-pressure@1.0.0` · Branch: `arena/agent4-p9.9-resource-pressure` (stacked on P9.8 `d80c73f0`)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-resource-pressure.test.mjs` — **11 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| Resource dimensions observable | `resourceFixtures` (cpu/memory/disk/network/queue/…) | levels match default policy thresholds |
| OBSERVED / ESTIMATED / REPORTED distinguishable | provenance test + `latestSnapshot()` | three provenances retained per dimension; fabricated values rejected |
| Pressure states NORMAL/PRESSURED/CONSTRAINED/CRITICAL/UNKNOWN | `pressureTransitions` + collector failure test | full ladder exercised; UNKNOWN on collector failure |
| Thresholds policy-driven | `policyThresholds` + `setThresholds` | tighter memory policy reclassifies 0.55 → PRESSURED; invalid policy rejected |
| Readings never unbounded | `unboundedGuard` (1000 readings, history 8) | historyCount stuck at 8; overflow drops counted |
| Pressure drives degradation | `degradationHook` + `DEGRADATION_LEVELS` | CRITICAL → level 3 fired; recovery → 0; throwing hook safe |
| Collector fails safely | throwing collector | `collect()` → UNKNOWN + `collectorFailures`; recovery via new reading |
| Low-resource environment test | `lowResource` flood (200 @ historySize 16) | CRITICAL state, history ≤ 16, peakDegradation 3 |

## Full gates (with `N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| backend full | **1477 pass / 0 fail** |
| dedicated resource-pressure | **11 pass / 0 fail** |
| `lego:arch` | OK — 26 domains / **62 contracts** |
| `lego:arch:selftest` | 26/26 |
| `lego:foundation` | OK |
| `lego:foundation:selftest` | 15/15 |
| `lego:capabilities` | OK |
| `lego:scaleout` | OK |
| `lego:ai:check` | OK — 64 gen / 37 curated / 101 total |
| frontend full | **418 pass / 0 fail / 1 skip** |

## Bugfixes during slice (do not regress)

1. **Fixture key strip** — resource fixture rows carry `expectLevel` (test metadata); `recordReading` correctly rejects unknown keys — test strips before record (do not loosen the allowlist).
2. **JSON `undefined`** — `fabricatedRejected` initially included bare `undefined` (invalid JSON); removed — test still covers non-string provenance via `42`/`null` and in-code `undefined` is covered by hostile-input list.
3. **Policy fixture `note`** — `fixture.policyThresholds` includes a prose `note` key; tests pass only `.memory` into `setThresholds` (threshold validator rightly rejects non-dimension keys).

## Classification decisions (not bugs)

- `UNKNOWN` maps to degradation 0 but is a **distinct state** — operators must read `state`/`collectorFailures`, not degradation alone, to tell “no pressure” from “telemetry blind”.
- Default `currentState` starts `NORMAL` (optimistic) until first reading or collector failure — empty monitor is not CRITICAL.
