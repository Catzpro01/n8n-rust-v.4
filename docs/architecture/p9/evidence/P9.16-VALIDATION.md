# P9.16 Validation Evidence — Operator Inspection API

Slice: P9.16 · Contract: `observability.operator-inspection@1.0.0` · Branch: `arena/agent4-p9.16-operator-inspection` (stacked on P9.15 `66135ed5` / PR #196)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-operator-inspection.test.mjs` — **9 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| Interfaces as authorized | normal query × 6 interfaces | all `ok` |
| Pagination | large query page walk | total = dataset; pages ≥ 2; limit ceiling |
| Query timeouts | `timeoutQuery` + advancing `ctl.now` | `operator.timeout` |
| Cancellation | cancel hook after 3 scans | `operator.cancelled` |
| Bounded results | `OPERATOR_LIMITS.maxLimit` rejections | `operator.invalid_query` over max |
| Access policy | guest / unknown / missing role | `operator.unauthorized` (×3) |
| Does not block workflow runtime | pure dataset queries; no I/O; workflow never imports module | fail-closed, never throws |
| Indexed bounded strategy | equality `where` → `usedIndex: true` | error-filter count matches dataset |
| Stable machine-readable errors | all failure paths assert `error.code` | enum from `OPERATOR_ERRORS` |

## Required evidence fixtures

| Case | Fixture key | Result |
|---|---|---|
| Normal query | `normalQuery` | 10 rows, bounded |
| Large query | page walk | multi-page, max 500/page |
| Timeout | `timeoutQuery` + ctl | `operator.timeout` |
| Cancellation | `cancelQuery` + ctl | `operator.cancelled` |
| Unauthorized query | `unauthorizedQuery` | `operator.unauthorized` |
| Concurrent query storm | `storm` + nested reentry | `operator.busy` under maxInFlight=1 |

## Full gates (`N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| dedicated operator-inspection | **9 pass / 0 fail** |
| backend full | **1551 pass / 0 fail** |
| frontend full | **418 pass / 0 fail** (1 skipped, pre-existing) |
| `lego:arch` | **OK — 26 domains, 69 contracts** |
| `lego:arch:selftest` | **26/26** |
| `lego:foundation` | **OK — 26 LEGOs, 12 node creation routes** |
| `lego:foundation:selftest` | **15/15** |
| `lego:capabilities` | **OK — 23 REST features vs 91 registered capabilities** |
| `lego:scaleout` | **OK — all findings declared/owned** |
| `lego:ai:check` | **OK — Generated 64 / curated 37 / total 101; Locked contracts 69** |

## Fixes during slice (do not regress)

1. **Never throws** — every query returns `{ok, result|error}` envelopes only.
2. **Storm → busy, not queue-forever** — `maxInFlight` rejects with `operator.busy`.
3. **Timeout/cancel via ctl** — deterministic in tests; production callers can wire real clocks.
4. **Access policy explicit** — grants table; guest/unknown/missing denied.
5. **Contract-lock 68→69** — clean append; count-pins 68→69; `33-milestones` finishEvidence stays 65.
6. **Arch/F16** — sole import `telemetry-redaction`; no bare unpublished namespace.snake strings.

## Classification decisions (not bugs)

- Unknown interface/field → `operator.invalid_query` (not unauthorized).
- `limit > maxLimit` → invalid, not truncated silently.
- Index miss falls back to bounded full scan (`usedIndex: false`).
