# P9.21 Validation Evidence — Advanced Diagnostics / Incident Intelligence

Slice: P9.21 · Contract: `observability.advanced-diagnostics@1.0.0` · Branch: `arena/agent4-p9.21-advanced-diagnostics` (stacked on P9.20 `a2743bee` / PR #204)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-advanced-diagnostics.test.mjs` — **11 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| Feature matrix | `featureMatrix` (6 rows) | budget+kill+fallback+pressure+AB per row |
| Trade-off matrix | `tradeOffMatrix` | gain/cost/risk/mitigation per feature |
| Pressure tests | flood test | all budgets held under 100–200 op floods |
| Kill-switch tests | construction `disabled` + runtime `setKillSwitch` | all 6 features switchable |
| Fallback tests | slow-path / absolute / passthrough / undeduped / keep / steady | each path asserted |
| Workflow A/B | inputs × normal/stressed/all-disabled | `deepEqual` results |
| Benchmark report | § Benchmark below | ops/sec hot paths |
| Security/audit never deduped | exempt walk (security ×5, audit) | always admitted |
| Profiling/eBPF optional | `notSelected` | not implemented, documented |
| Tail sampling bounded | `maxInFlight=3` flood | in-flight never exceeds 3; overflow keeps |

## Benchmark report (microbench, node 22 sandbox, 2026-09-24)

Method: warmup 1k, then time 100k ops per path via `process.hrtime.bigint`.

| Path | Ops | Result |
|---|---|---|
| `admitFast` happy path | 100_000 | **649,574 ops/sec** |
| `intern` hit path | 100_000 | **3,020,060 ops/sec** |
| `offerDedup` suppress path | 100_000 | **1,685,287 ops/sec** |
| `observeDelta` steady state | 100_000 | **2,463,607 ops/sec** |
| `workflowAb` pure transform | 100_000 | **2,119,093 ops/sec** |

## Full gates (`N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| dedicated advanced-diagnostics | **11 pass / 0 fail** |
| backend full | **1602 pass / 0 fail** |
| frontend full | **418 pass / 0 fail (+1 skip)** |
| `lego:arch` | **OK — 26 domains, 74 contracts** |
| `lego:arch:selftest` | **26/26** |
| `lego:foundation` | **OK — 26 LEGOs, 12 node creation routes** |
| `lego:foundation:selftest` | **15/15** |
| `lego:capabilities` | **OK — 23 REST features vs 91 registered capabilities** |
| `lego:scaleout` | **OK — all findings declared/owned** |
| `lego:ai:check` | **OK — Generated 64 / curated 37 / total 101; Locked contracts 74** |

## Fixes during slice (do not regress)

1. **Security/audit exempt short-circuits first** — never suppressed while feature on; kill-switch admits everyone undeduped.
2. **Tail overflow returns keep without tracking** — in-flight Map never exceeds `maxInFlight`.
3. **Burst window hard cap** `threshold*4` — pathological floods cannot grow the array.
4. **Delta overflow is typed error** — baselines never exceed `deltaBaselineMax`.
5. **Contract-lock 73→74** — clean append; count-pins 73→74.
6. **Arch/F16** — sole import `telemetry-redaction`; no bare unpublished namespace strings.

## Classification decisions (not bugs)

- Negative delta on counter decrease is honest — reset detection is a consumer concern.
- `tailEnd` on unknown id → idempotent keep (`tracked:false`).
- Kill-switch on fast_path still accepts (`accepted:true`, path `disabled`) — disable must not lose data.
