# P9.17 Validation Evidence — Low-Resource Observability Mode

Slice: P9.17 · Contract: `observability.low-resource-mode@1.0.0` · Branch: `arena/agent4-p9.17-low-resource` (stacked on P9.16 `d78cc317` / PR #198)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-low-resource-mode.test.mjs` — **10 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| Auto or explicit activation | `hysteresis` + `explicit` samples | `pressure-enter` / explicit works |
| Defined degrade order | full step walk | equals `LOWRES_DEGRADE_ORDER` |
| Budgets enforced | byte flood + cpu spike | `budget_shed` + counters |
| Workflow A/B correctness | `workflowAb.inputs` × modes | `deepEqual` results |
| Memory reduction measurable | flood admit cap = budget/size | exact admitted count |
| CPU reduction measurable | over-`cpuBudgetPct` shed counters | `budgetCpuEnforced` |
| Hysteresis/cooldown | flip-flop within cooldown | `thrashBlocked` ≥ 2 |
| Critical/security protected | `audit_security` admits at floor/over budget | `protected` |
| Recovery tested | cooldown-elapsed low pressure | `NORMAL`, `channelsOff` empty |

## Required evidence fixtures

| Case | Fixture key | Result |
|---|---|---|
| Memory pressure benchmark | `memoryPressure` + flood | admitted = floor(budget/200) |
| CPU pressure benchmark | `cpuPressure` | enter + cpu shed / protected |
| Mode transition test | `hysteresis.samples` | enter 80% → exit ≤50% after cooldown |
| Recovery test | t=10000 low sample | NORMAL + channels restored |
| Workflow A/B correctness | `workflowAb.inputs` | identical results across modes |

## Full gates (`N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| dedicated low-resource-mode | **10 pass / 0 fail** |
| backend full | **1561 pass / 0 fail** |
| frontend full | **418 pass / 0 fail** (1 skipped, pre-existing) |
| `lego:arch` | **OK — 26 domains, 70 contracts** |
| `lego:arch:selftest` | **26/26** |
| `lego:foundation` | **OK — 26 LEGOs, 12 node creation routes** |
| `lego:foundation:selftest` | **15/15** |
| `lego:capabilities` | **OK — 23 REST features vs 91 registered capabilities** |
| `lego:scaleout` | **OK — all findings declared/owned** |
| `lego:ai:check` | **OK — Generated 64 / curated 37 / total 101; Locked contracts 70** |

## Fixes during slice (do not regress)

1. **Hysteresis required** — `exit < enter` at construction; else null.
2. **Cooldown is absolute** — any transition (auto or explicit) inside `cooldownMs` → `thrashBlocked`.
3. **audit_security never shed** — `protected` even over budget (`budgetByteEnforced` may go >0 honestly).
4. **Whole-record budget_shed** — no partial writes / corruption.
5. **workflowAb purity** — transform(input) only; mode is metadata.
6. **Contract-lock 69→70** — clean append; count-pins 69→70; `33-milestones` finishEvidence stays 65.
7. **Arch/F16** — sole import `telemetry-redaction`; no bare unpublished namespace.snake strings.

## Classification decisions (not bugs)

- `exit >= enter` config → constructor null (not a runtime surprise).
- Explicit mode change during cooldown → `changed:false, reason:'cooldown'` (not an error).
- Over-budget security admit → `protected` + enforced counter (degraded floor, not corruption).
