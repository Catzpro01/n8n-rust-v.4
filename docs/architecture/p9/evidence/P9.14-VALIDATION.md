# P9.14 Validation Evidence — Replay / Compatibility Evidence Integration

Slice: P9.14 · Contract: `observability.replay-evidence@1.0.0` · Branch: `arena/agent4-p9.14-replay-evidence` (stacked on P9.13 `f8c0371c`)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-replay-evidence.test.mjs` — **13 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| Consume refs from owning domains | `p3Checkpoint` + `p6Compatibility` | both compile, version-checked against pinned formats |
| No duplicate replay engines | purity test | sole import `./telemetry-redaction.mjs`; no `stateStreamFromSnapshot` / `replayCompatibility` / `replayFingerprints` |
| Missing refs safe | `missingReference` | `evidenceMissing: ['replayRefs']`, empty evidence |
| Refs in diagnostic bundles | `selectReplayRefsForBundle` + `attachReplayEvidence` | P3→`checkpoint`, P6→`replay`; artifacts preserved |
| P3/P6 formats version-checked | `incompatibleVersion` + `incompatibleContract` | segregated under `incompatible`, never in `evidence` |
| Secret-safe | `secretRef` + export test | compile `null`; export refuses secret-shaped body |
| End-to-end correlation | `endToEnd` | diagnostic `executionId` = failure-correlation `rootId`, rootKind `OBSERVED_CAUSE` |

## Required evidence fixtures

| Case | Fixture key | Result |
|---|---|---|
| P3 replay reference | `p3Checkpoint` | compatible · bundle slot `checkpoint` |
| P6 compatibility replay | `p6Compatibility` | compatible · verdict annotation `DIFF` carried, engine not re-run |
| Missing reference | `missingReference` | `evidenceMissing` includes `replayRefs` |
| Incompatible version | `incompatibleVersion` | `snapshotVersion-mismatch` · expected 1, actual 2 |

## Full gates (`N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| dedicated replay-evidence | **13 pass / 0 fail** |
| backend full | **1532 pass / 0 fail** |
| frontend full | **418 pass / 0 fail** (1 skipped, pre-existing) |
| `lego:arch` | **OK — 26 domains, 67 contracts** |
| `lego:arch:selftest` | **26/26** |
| `lego:foundation` | **OK — 26 LEGOs, 12 node creation routes** |
| `lego:foundation:selftest` | **15/15** |
| `lego:capabilities` | **OK — 23 REST features vs 91 registered capabilities** |
| `lego:scaleout` | **OK — all findings declared/owned** |
| `lego:ai:check` | **OK — Generated 64 / curated 37 / total 101; Locked contracts 67** |

## Fixes during slice (do not regress)

1. **References-not-engines** — P9 only version-checks contract id + version field; never restores snapshots or re-runs compatibility replay.
2. **Incompatible ≠ evidence** — version/contract mismatches land in `incompatible[]`, never promoted to `evidence[]`.
3. **Missing is data** — empty/absent `refs` yields `evidenceMissing`, not a throw.
4. **Secret fail-closed** — bearer/password/token shapes null the compile and the export.
5. **Contract-lock row count 66→67** — appended `observability.replay-evidence@1.0.0` (clean append). Shared `.length` count-pins bumped to 67 (value form `, 66` / `, 66,` and `count-pins say 66` messages). `33-milestones.test.mjs` `finishEvidence.pr` stays at 65 (unrelated milestone count).
6. **Arch-safe pins** — sole import `telemetry-redaction` (observability must not import execution); format pins stored as local data matching owners.
7. **F16-safe** — bare contract ids kept inside full `@version` strings (even in comments).

## Scale stress

`lego-scale-stress.test.mjs` (1M nodes, issue #75) — **6 pass / 0 fail** when run alone (~21s on this ~2 GB runner); file-level flake under parallel duplicate load only, not a P9.14 regression.

## Classification decisions (not bugs)

- Unknown `source` → compile `null` (invalid, not incompatible).
- Unknown key on a ref → compile `null` (SPEC_KEYS-style fail closed).
- Oversize ref list (>16) → compile `null`.
- Optional `digest` must be 64-hex if present (shape only — not re-hashed here).
- Optional `verdict` must be one of MATCH/DIFF/NON_DETERMINISTIC/MISSING.
