# P9.13 Validation Evidence — Failure Correlation + Causal Evidence

Slice: P9.13 · Contract: `observability.failure-correlation@1.0.0` · Branch: `arena/agent4-p9.13-failure-correlation` (stacked on P9.12 `044c5ff1`)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-failure-correlation.test.mjs` — **12 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| Four cause kinds represented | vocabulary test + all fixtures | `OBSERVED_CAUSE` / `INFERRED_CAUSE` / `CORRELATED_WITH` / `UNKNOWN` |
| Evidence references attached | direct-chain + `attachEvidence` | observed edges carry `evidence[]`; unsafe refs → null |
| Causal edges bounded | bounded-inputs test | `maxEdges` 256 / `maxEvents` 128; oversize → null |
| Cycles do not infinite expansion | `cycleProtection` fixture | `cycleDetected: true`, `terminates: true`, path ≤ node count |
| Unsupported causality not promoted | `correlationOnly` fixture | root `UNKNOWN` + `correlation-only-not-promoted` |
| confidence/source where inference used | `confidenceSource` fixture | `confidence` 0.72, `source` `correlator.v2`, note `inferred-not-observed` |
| Unknown root cause honest | `unknownRoot` fixture | `UNKNOWN` + `no-inbound-causal-claims` + `causalEdges` in `evidenceMissing` |
| Representative fixtures pass | 5 required + 6 invalid | 5 green; all invalid fail closed to null |

## Full gates (`N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| dedicated failure-correlation | **12 pass / 0 fail** |
| backend full | **1519 pass / 0 fail** |
| frontend full | **418 pass / 0 fail** (1 skipped, pre-existing) |
| `lego:arch` | **OK — 26 domains, 66 contracts** |
| `lego:arch:selftest` | **26/26** |
| `lego:foundation` | **OK — 26 LEGOs, 12 node creation routes** |
| `lego:foundation:selftest` | **15/15** |
| `lego:capabilities` | **OK — 23 REST features vs 91 registered capabilities** |
| `lego:scaleout` | **OK — all findings declared/owned** |
| `lego:ai:check` | **OK — Generated 64 / curated 37 / total 101; Locked contracts 66** |

## Fixes during slice (do not regress)

1. **Fixture `expect` key** — fixtures carry a non-contract `expect` annotation; `compileInput` strips it before compile (SPEC_KEYS fail-closed on unknown keys).
2. **Correlation promotion** — `CORRELATED_WITH` alone never yields a causal rootKind; always `UNKNOWN` + note.
3. **Cycle expansion** — visited-set guarantees termination; `cycleDetected` set when a parent was already visited.
4. **Secret evidence** — `boundedId` runs `containsSecretShape` + bearer/password patterns; secret evidence → compile null / attach null.

## Classification decisions (not bugs)

- Self-loop edges (`from === to`) → null.
- Unknown event refs on edges → null.
- `confidence` outside [0,1] or non-finite → null.
- `INFERRED_CAUSE` without `source` → null.
- `OBSERVED_CAUSE`/`CORRELATED_WITH` without evidence → null.
- `confidence`/`source` on `OBSERVED_CAUSE` → null (shape illegal).
