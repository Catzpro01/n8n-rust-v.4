# P9.18 Validation Evidence — Telemetry Self-Observability

Slice: P9.18 · Contract: `observability.self-observability@1.0.0` · Branch: `arena/agent4-p9.18-self-observability` (stacked on P9.17 `b534c08a` / PR #200)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-self-observability.test.mjs` — **10 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| Ten required counters reported | `pipelineSequence` notes | all ≥ 1 after note |
| Zero errors vs zero telemetry | three-phase classification test | independent booleans, 4-way class |
| Self-metrics bounded | fixed keys + reject unknown kind | exactly 10 keys; no dynamic labels |
| No infinite recursion | `onNote` re-entry | `selfview.recursion`; generated ×1 |
| Own suppression/priority rule | outage P3/P1/P0 walk | P2–P4 suppressed, P0/P1 delivered |
| Simulated telemetry outage | 5 × `attemptExport(false)` | `export_failed=5`, self-notes continue |
| Drop-counter test | 7 offers @ capacity 4 | buffered 4 / dropped 3 exact |
| Exporter failure | threshold 3 | `degraded ≥ 1`, flags clear on success |

## Required evidence fixtures

| Case | Fixture key | Result |
|---|---|---|
| Simulated telemetry outage | `outage.failures=5` | export_failed 5, outage true, recovery clears |
| Drop-counter test | `drop` (cap 4, offers 7) | buffered 4, dropped 3 |
| Exporter failure | `outage.degradeAfter=3` | degraded once at 3 consecutive |
| Recursion guard | re-entrant `onNote` | suppressed with `selfview.recursion` |

## Full gates (`N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| dedicated self-observability | **10 pass / 0 fail** |
| backend full | **1571 pass / 0 fail** |
| frontend full | **418 pass / 0 fail (+1 skip)** |
| `lego:arch` | **OK — 26 domains, 71 contracts** |
| `lego:arch:selftest` | **26/26** |
| `lego:foundation` | **OK — 26 LEGOs, 12 node creation routes** |
| `lego:foundation:selftest` | **15/15** |
| `lego:capabilities` | **OK — 23 REST features vs 91 registered capabilities** | |
| `lego:scaleout` | **OK — all findings declared/owned** | |
| `lego:ai:check` | **OK — Generated 64 / curated 37 / total 101; Locked contracts 71** |

## Fixes during slice (do not regress)

1. **Recursion guard is re-entrancy, not a latch** — after `onNote` returns, fresh top-level notes work.
2. **Counters always bump under suppression** — only detail delivery is suppressed.
3. **Attempt-export failure is boolean-only** — invalid args fail-closed.
4. **Contract-lock 70→71** — clean append; count-pins 70→71.
5. **Arch/F16** — sole import `telemetry-redaction`; no bare unpublished namespace strings.
6. **take() clamps** to occupancy; never negative.

## Classification decisions (not bugs)

- Idle (both zero-flags true) ≠ healthy traffic (`zeroTelemetry=false, zeroErrors=true`) — by design.
- `degraded` flag clears on export success; cumulative `degraded`/`export_failed` counters never reset.
- Default priority P2 is a detail class — suppressed during outage unless caller passes P0/P1.
