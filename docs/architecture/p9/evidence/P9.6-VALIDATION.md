# P9.6 validation — 2026-09-24

Baseline main: `24032a0ccdb388c4e81b87f3e8c11220fb681feb`.
Parent slice: P9.5 `0b8bd9e2` (stacked branch `arena/agent4-p9.6-telemetry-buffer`).
Node (sandbox), Linux x64, shared host.

## Coverage

9 dedicated tests: contract/priority/required-capacity matrix (no default →
no silent unbounded queue); queue saturation to hard ceiling (1000 offers →
size 8, explicit outcomes only); priority shedding (P0 evicts only strictly
lower P4, never equal P0/P1; equal-priority incoming sheds);
`reject_incoming` deterministic FIFO protection; drop observability
(buffered+shed+rejected = offerTotal exactly; per-class shedP* and
evictedP*; occupancy exposed); malformed/proxy/throwing-payload records
reject without accessor reads or exceptions; **workflow A/B under
saturation** (identical business result with no buffer / healthy / 10k
saturated offers); 50-cycle overload memory budget (peakSize ≤ capacity,
source scan: no timers/fs/fetch, only `new Array(capacity)` wall);
locked row + doc conformance (count-pin 59).

Commands:
- `node --test apps/n8n-lego/test/lego-telemetry-buffer.test.mjs` → **9 pass / 0 fail**
- `N8N_LEGO_CATALOG_DIR=/home/user/.n8n-lego/catalog node --test apps/n8n-lego/test/*.test.mjs` → **1446 pass / 0 fail**
- frontend `418 pass / 0 fail / 1 existing skip`
- arch (59 contracts) + foundation + capabilities + scaleout + ai:check **OK**
- `npm run lego:ai` regeneration after domains P9.6 / contract 1.5.0

Shared-surface exact delta (this slice only):
- `contract-lock.json` +1 `observability.telemetry-buffer@1.0.0` (58 → 59).
- Count-pins 58 → 59 in 18 files with explicit P9.6 suffix; none deleted.
- `domains.json` observability: phase P9.5 → P9.6, contract 1.4.0 → 1.5.0,
  +path/+public/+test `telemetry-buffer`. **Owner stays `agent-6`.**
- `BACKEND_LEGO.md` +1 changelog row. `.ai` via ai-pack.

Accounting invariant proven: every `offer` is exactly one of
`buffered|shed|rejected` (`offerTotal`); eviction increments `evicted`/
`evictedP*` without double-counting the victim's original `buffered`.

## Spill-to-disk

Not implemented (DoD: "if implemented"). 1.0.0 is in-memory only; a future
quota-bounded spill is an additive version decision.

## Benchmark

Raw: `p9.6-buffer-benchmark.json`; script `tools/p9/telemetry-buffer-benchmark.mjs`.
Microbaseline only — admission path on shared sandbox; not workflow
throughput, no exporter/disk I/O measured.
