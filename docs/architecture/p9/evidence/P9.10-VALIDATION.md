# P9.10 Validation Evidence — Health / Readiness Model

Slice: P9.10 · Contract: `observability.health-readiness@1.0.0` · Branch: `arena/agent4-p9.10-health-readiness` (stacked on P9.9 `9b17949e`)

## Dedicated suite

`node --test apps/n8n-lego/test/lego-health-readiness.test.mjs` — **11 tests / 0 fail** (final).

## DoD evidence matrix

| DoD requirement | Fixture / test | Result |
|---|---|---|
| LIVENESS and READINESS distinct | axis-independence test | `ALIVE/DEAD` vs `READY/NOT_READY` separate fields; kill probe flips liveness without merging axes |
| DEGRADED and UNAVAILABLE distinct | `degradedDependencyFixture` sequence + condition fixtures | cache degraded → DEGRADED; critical down → UNAVAILABLE; never collapse |
| Structured reason codes | `reasonCodes` fixture = `HEALTH_REASON_CODES` (14) | critical/degraded/healthy paths assert exact codes |
| Startup/recovery transitions | `startupTransitions` + `recoveryTransitions` | STARTING→RUNNING; RUNNING→RECOVERING→RUNNING; invalid transitions return false/null |
| False-healthy rejected | `falseHealthy` + false-healthy test | RUNNING + critical down → NOT_READY/UNAVAILABLE + `false_healthy.rejected`; `falseHealthyAttempts ≥ 1` |
| Bounded cheap queries | `boundedQuery` (64 deps × 1000 evaluates) | reasons ≤ 32, deps ≤ 64, query counter exact |
| Health telemetry available during degradation | report-availability test | UNAVAILABLE and DEGRADED still return full frozen structure + stats |

## Full gates (`N8N_LEGO_CATALOG_DIR` catalog)

| Gate | Result |
|---|---|
| backend full | **1488 pass / 0 fail** |
| dedicated health-readiness | **11 pass / 0 fail** |
| `lego:arch` | OK — 26 domains / **63 contracts** |
| `lego:arch:selftest` | 26/26 |
| `lego:foundation` | OK |
| `lego:foundation:selftest` | 15/15 |
| `lego:capabilities` | OK |
| `lego:scaleout` | OK |
| `lego:ai:check` | OK — 64 gen / 37 curated / 101 total |
| frontend full | **418 pass / 0 fail / 1 skip** |

## Classification decisions (not bugs)

- No-dependency RUNNING model reports HEALTHY/READY (empty table = nothing to be down) — documented default.
- Degraded non-critical dependency ⇒ readiness `NOT_READY` (no *new* work while guarantees are reduced); liveness stays `ALIVE`.
- Reason entries may be `code` or `code:detail` — both match `HEALTH_REASON_CODES` prefix.
