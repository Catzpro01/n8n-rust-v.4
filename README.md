# n8n-rust-v.4

High-Performance Rust Port of n8n with Arena AI Virtual SSH Engine.

## Structure
- `tasks/` : Inbound task manifests from AI Arena agents (`TASK-xxx.yaml`)
- `results/` : Execution output and logs from VPS (`TASK-xxx.md`)
- `crates/` : Core n8n Rust engine modules
