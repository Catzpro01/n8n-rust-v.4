# P4.2 Evidence — Activation + Generation Lifecycle

Slice: P4.2 · Issue: [#104](https://github.com/Catzpro01/n8n-rust-v.4/issues/104)
· Engine: `crates/n8n-common/src/activation.rs` (runtime +
`#[cfg(test)]` suite).

## Hasil test (rig offline, sesi build)

- Rig: `tools/rust-offline-rig/run.sh test` — **exit 0**, seluruh workspace hijau.
- Total test: **203** (181 sebelum slice + **22 baru**), failures: **0**.
- `n8n-common` lib suite: **52 passed; 0 failed** (30 kontrak P4.1 + 22 lifecycle P4.2).
- Fungsional baru tervalidasi langsung di log rig (grep `test activation::tests` = 22/22 ok).

## Coverage terhadap checklist #104

| Kewajiban #104 | Implementasi P4.2 | Bukti test |
|---|---|---|
| activate/deactivate idempotency | `activate()` → AlreadyActive/AlreadyActivating; `deactivate()` → AlreadyDraining/AlreadyDeactivating/NotActive | `activate_surface_is_idempotent_like_n8n`, `activate_on_activating_is_serialized`, `deactivate_unknown_is_not_active_like_n8n`, `graceful_deactivation_walks_drain_chain` |
| activation/update/deactivation races | CAS `expected: Option<Generation>` di semua command; baca-ulang wajib saat `StaleCommand`; serialization boundary `AlreadyActivating` | `cas_expected_generation_blocks_racing_commands`, `deactivate_is_cas_guarded` |
| generation/epoch stale-event protection | `may_serve` = serving-state + `fence_generation` exact-match; retry/update menerbitkan generation baru monotonic | `old_activation_cannot_serve_after_new_generation_commits`, `may_serve_is_the_fail_closed_serving_gate` |
| partial activation rollback | `fail_activation` (→Failed, error terekam), `abort_activation` (→Inactive bersih), deactivate-pada-Activating → `CancelledActivation` | `failed_activation_retries_with_new_generation`, `deactivating_during_activation_cancels_cleanly` |
| graceful drain | `begin_drain` (deadline wajib, type-level) → `finish_drain` → teardown; draining menolak serving baru | `graceful_deactivation_walks_drain_chain` |
| restart reconciliation | `reconcile(desired) -> ReconcilePlan` deterministik, 4 bucket + urutan leksikografik | `reconcile_produces_a_deterministic_plan`, `reconcile_leaves_serving_workflows_alone` |
| persisted active workflows | desired-set adalah kontrak input reconcile; records = projection (durasi diikat journal P4.7) | doc §2/§4 + test reconcile |
| stale trigger/webhook/schedule cleanup | `to_deactivate` bucket memaksa penurunan; fence membuat route basi mustahil melayani | `reconcile_produces_a_deterministic_plan`, serving-gate tests |
| leader change | `owner_instance` tercatat (bukan election); failover defer P4.7 | doc §6 |
| recovery after crash | journalling + replay defer P4.7 (disesuaikan dengan batas slice master prompt) | doc §6 |
| activation-error visibility | `record.last_error` (message/node/at_ms), dibersihkan saat commit/recover — parity `GET /rest/active-workflows/error/:id` | `failed_activation_retries_with_new_generation`, `degraded_escalates_and_recovers_deterministically` |

## Skenario race/failing paths khusus (Gate F)

- duplicate request → fence exact-match tolak gen lama ✅
- stale generation → `StaleCommand` CAS ✅
- activation interruption (deactivate di tengah Activating) → `CancelledActivation` ✅
- retry → generation baru ✅
- invalid state transition → `IllegalTransition` terstruktur (22 path diuji: perimeter
  table-driven ada di kontrak P4.1; runtime menegakkannya per command) ✅
- failed teardown → `Failed` (terlihat rekonsiliasi) ✅

## Kompatibilitas / batas

- Nol perubahan `ingress.contracts@0.1.0` (frozen); nol diff di luar
  `crates/n8n-common/src/{activation.rs,lib.rs}` + dokumen ini +
  progress log. `cargo metadata` unchanged (std-only, `BTreeMap`).
- Registry `sync`, single-writer; locking multi-writer = tanggung jawab host
  (dok §2). Tanpa channel/thread supaya deterministik & teruji.
