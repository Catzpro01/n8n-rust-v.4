# P4.7 Evidence — Recovery, Reconciliation & Race Safety (Issue #107·#104)

Slice: P4.7 · Modul: `crates/n8n-common/src/recovery.rs` (plane + suite test).

## Hasil test (rig offline, exit 0)

- **269 tests green** workspace (262 P4.6 → +7 P4.7), 0 failures, 0 warnings.
- File slice lolos `cargo fmt` (rustfmt 1.8.0-stable).

## Coverage

| Spesifikasi | Implementasi | Test |
|---|---|---|
| crash recovery (journal replay) | `Journal` bounded replayable | `journal_is_replayable_and_bounded`, `journal_replay_from_returns_entry_after_seq` |
| leader change/failover | `LeaderLease` epoch/holder/expiry | `lease_acquire_renew_expire_release`, `lease_asserts_reject_wrong_and_expired` |
| interrupted activation (stalled) | `ActivatingStalled` + grace | `recovery_detects_stalled_activation` |
| stalled drain/deactivating | `ResumeDrain` | `recovery_plan_orders_and_classifies_states` |
| orphan serving | `DeactivateOrphan` | ibid |
| failed desired → retry | `RetryActivation` | ibid |
| desired missing → activate | `ReactivateDesired` | ibid |
| determinism | sort canonical + `ReconcilePlan` | `recovery_plan_is_deterministic` |

## Gate-F (race/failure)

lease salah-holder/expired/vacant (fail-closed); journal drop-oldest tanpa
overflow; stall-detection grace; recovery action idempotent & urut.

## Batas

- Nol perubahan kontrak; nol dependensi (VecDeque std); nol IO; diff terbatas
  ke `crates/n8n-common/src/{recovery.rs,lib.rs}` + dokumen.
- Pemilihan pemimpin tetap host; tidak ada jam terdistribusi.
