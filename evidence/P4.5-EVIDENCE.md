# P4.5 Evidence — Ingress Admission, Backpressure & Idempotency (Issue #106)

Slice: P4.5 · Modul: `crates/n8n-common/src/admission.rs` (plane + suite test).

## Hasil test (rig offline, exit 0)

- **252 tests green** workspace (239 P4.4 → +13 P4.5), 0 failures, 0 warnings.
- File slice lolos `cargo fmt` (rustfmt 1.8.0-stable).

## Coverage checklist #106 (outcome machine + bounds)

| Kewajiban #106 | Implementasi | Test |
|---|---|---|
| 8 outcome verbatim (Accepted/Queued/…/Unavailable) | `AdmissionState` P4.1 dipakai langsung; mesin memilih state per langkah | berbagai test di bawah |
| request/correlation ID, workflow/version, trigger id, tenant, deadline, priority, idempotency key, security ref | seluruhnya ada di `IngressEnvelope` (kontrak P4.1); admission memakainya | `valid_envelope_is_admitted` |
| bounded ingress queues | `max_in_flight` (shed saat penuh) | `overload_sheds_when_in_flight_exceeded`, `finish_execution_releases_fast` |
| per-route/per-workflow/global limits | per-route token-bucket + global in-flight + payload | `rate_limit_is_enforced_and_retryable` |
| body-size & concurrency limits | `max_body_bytes`/`max_payload_bytes`/cap inline | `oversized_payload_is_rejected` |
| producer throttling / retry-after | `retry_after_ms` deterministic + `retryable` | `rate_limit_is_enforced_and_retryable` |
| load shedding | overload → Unavailable (preserve health) | `overload_sheds_when_in_flight_exceeded` |
| health-preserving admission | config fail-closed; tidak ada alokasi tak terbatas | `config_rejects_impossible_values` |
| propagation into P3 execution boundary | `AdmissionAccepted.envelope` siap → `ExecutionRequest::from_envelope` (host) | `valid_envelope_is_admitted` |
| idempotency/replay | dedupe key kanonik ⇒ `Duplicate`+`duplicate_of` | `duplicate_idempotency_key_is_suppressed` |
| deadline | expired ⇒ `Expired`/DEADLINE_EXCEEDED | `expired_envelope_is_rejected_with_deadline_exceeded` |
| security fail-closed | hanya Allow; Deny/Indeterminate ⇒ Rejected/AUTH_DENIED | `denied_security_is_fail_closed` |
| generation fence | mismatch ⇒ Rejected/STALE_GENERATION | `stale_generation_is_rejected` |
| malformed | `validate` gagal ⇒ Rejected/MALFORMED_REQUEST | `malformed_envelope_is_rejected` |
| inactive workflow | ⇒ Rejected/WORKFLOW_INACTIVE | `inactive_workflow_is_rejected` |
| schedule idempotency key stabil | `sched:{wf}:{node}:{fire_at}` | `canonical_idempotency_key_is_stable_for_schedule` |

## Gate-F (race/failure)

duplicate key (dedupe), stale generation (fence), rate bucket habis (retry), overload
(shed), payload over-cap, malformed, expired — semuanya deterministik dan
diuji; state pulih via `finish_execution` (no leak).

## Batas & kompatibilitas

- Nol perubahan kontrak P4.1/P4.2; nol dependensi baru (std-only hashmap/deque);
  nol IO; diff terbatas ke `crates/n8n-common/src/{admission.rs,lib.rs}` + dokumen.
- At-least-once default; **tidak** mengklaim exactly-once.
