# P4.3 Evidence — Webhook Ingress (Issue #103)

Slice: P4.3 · Modul: `crates/n8n-common/src/webhook.rs` (plane + suite test).

## Hasil test (rig offline, exit 0)

- **219 tests green** workspace (203 sebelum slice → +16 P4.3), 0 failures, 0 warnings.
- `n8n-common` lib suite: seluruhnya ok (log rig tidak memuat `FAILED`/error/warning).

## Coverage checklist #103

| Kewajiban #103 | Implementasi | Test |
|---|---|---|
| production/test webhook identity | prefix kind P4.1 (6) + prod/test table terpisah; keduanya mounted saat aktivasi | `register_mounts_test_and_prod_routes` |
| method/path matching | `RouteTable` per method, `parse_segments` literal/`:param`/`*` | `resolve_extracts_params_and_wildcard_tail` |
| path params | `:id` diekstrak ke `ResolvedRoute.params` |  ibid |
| route conflicts | `RouteConflict` fail-closed + rollback penuh | `duplicate_static_route_is_conflict_and_fail_closed` |
| registration/unregistration | register/commit/abort/fail/deactivate semua sinkron lifecycle dan unmount | `registration_failures_roll_back_activation`, `deactivation_unmounts_routes_so_stale_routes_cannot_serve`, `aborted_workflow_does_not_serve` |
| activation coupling | serving gate menggabungkan state aktivasi + fence generation | `serving_check` via `full_request_flow_accepts_and_acks` + `stale_envelope_is_not_replayed` |
| response modes | `ResponsePlan` (P4.1) → `build_response` (onReceived 200; lain 202) | `full_request_flow_accepts_and_acks` |
| status/headers/body | ACK deterministik: 403 deny, 404 unknown, 405 allow-header, 500 conflict | `security_deny_fails_closed`, `resolve_returns_not_found_and_method_not_allowed` |
| raw/binary request handling | payload JSON inline vs string inline (indepe) | `normalize_binary_body_becomes_string_inline` |
| timeout/body-size limits | `NormalizeLimits` (body 16MiB, URI 16KiB, header/query P4.1 cap) | `normalize_enforces_body_size_limit` |
| request normalization | `normalize_request`: path/query/headers/body → bounded | `normalize_splits_path_query_and_classifies_body` |
| execution correlation | handoff `ExecutionRequest` (wrapper P4.1) dengan request/generation/node | `process_webhook_request` emit `ExecutionRequestStub` |
| replay/idempotency | anti-replay-generation via fence exact-match (bagian ini); replay same-generation penuh = P4.5 | `stale_envelope_is_not_replayed` |
| error mapping | deterministik (404/405/403/500) | `resolve_returns_not_found_and_method_not_allowed`, `security_deny_fails_closed` |
| disabled/unpublished workflow behavior | record tiada → 404 | `aborted_workflow_does_not_serve` |
| stale route cleanup | `cleanup_stale_routes` (non-serving / gen mismatch / webhook_id basi) | `cleanup_stale_routes_removes_stale_generation_routes` |

## Gate-F scenarios (race/failure) tercakup

- interupsi aktivasi (`abort`) → route dilepas; gagal midway → rollback penuh;
  konflik route → fail-closed tanpa partial mount;
  request gen-lawas → `ServingDenied::StaleGeneration`; security deny → 403 tanpa handoff.

## Batas & kompatibilitas

- Nol perubahan kontrak P4.1/P4.2; nol dependensi baru; diff terbatas ke
  `crates/n8n-common/src/{webhook.rs,lib.rs}` + dokumen ini.
- No IO: modul tidak mengimpor http/net — adapter HTTP tetap di luar
  (mengikuti keputusan arsitektur batas `n8n-common`).
