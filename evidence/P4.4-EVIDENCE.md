# P4.4 Evidence — Schedule/Cron & Duplicate-Tick Control (Issue #105)

Slice: P4.4 · Modul: `crates/n8n-common/src/schedule.rs` (plane + suite test).

## Hasil test (rig offline, exit 0)

- **239 tests green** workspace (219 sebelum slice → +20 P4.4), 0 failures,
  0 warnings. File slice lolos `cargo fmt` (rustfmt 1.8.0-stable).

## Coverage checklist #105

| Kewajiban #105 | Implementasi | Test |
|---|---|---|
| timezone | `TzResolver` host-defined; kontrak memvalidasi bentuk IANA/offset | `validation_rejects_bad_inputs_before_activation` |
| DST | gap policy `ShiftForward`/`SkipTick`; fold = occurrence terakhir (tepat sekali) | `dst_gap_shift_forward_fires_first_real_minute`, `dst_gap_skip_tick_skips_occurrence`, `dst_fold_fires_exactly_once_at_latest_occurrence` |
| cron validation | parser std-only; 5-field + 6-field-detik-0; domain & token | `cron_parses_standard_forms`, `cron_accepts_six_field_with_zero_seconds_rejects_otherwise`, `cron_rejects_invalid_ranges_and_tokens` |
| misfire/missed-run | `MisfirePolicy::Skip`/`BackfillOnce` | `misfire_skip_defers_but_backfill_fires_once` |
| overlap/concurrency | `OverlapPolicy::Allow`/`Skip`/`QueueOnce`; `finish_execution` | `overlap_skip_blocks_tick_while_in_flight` |
| deadline propagation | `deadline_ms` → `ScheduleExecution` (cap) | `jitter_and_deadline_bounds_are_enforced` |
| leader ownership/failover | `owner_instance` +failover defer P4.7 (didokumentasikan) | doc §6 |
| duplicate tick suppression | anchor `last_fired_at_ms` + `fired_ticks` bounded | `duplicate_tick_for_same_instant_is_suppressed` |
| schedule version/generation | generation baru per aktivasi; fence fail-closed | `register_commit_and_tick_fires_once`, `deactivation_removes_schedules_and_update_flows` |
| update without duplicate registration | drain→teardown→re-register | `deactivation_removes_schedules_and_update_flows` |
| persistence | journal P4.7 (didok); orphan reconcile disediakan | `recover_removes_orphan_schedules` |
| jitter | cap `MAX_JITTER_MS` | `jitter_and_deadline_bounds_are_enforced` |

## Semantik DOM/DOW & kalender

- `cron_handles_dom_dow_or_semantics` (1 ATAU Senin), `next_fire_leap_day_only_fires_on_leap_feb29`,
  `impossible_cron_returns_none_fail_closed`, `epoch_days_and_dow_are_consistent`
  (days-from-civil roundtrip, 1970=0, 2024-02-29).

## Gate-F (race/failure)

- clock regresi → duplicate suppressed; missed-run → skip/backfill deterministik;
  overlap → Skip/QueueOnce bounded; schedule lawas (generation berbeda) → fence gagal;
  pola cron mustahil → None fail-closed; pola crack sebelum aktivasi (bad tz) →
  gagal sebelum record/registry berubah.

## Batas & kompatibilitas

- Nol perubahan kontrak P4.1/P4.2; nol dependensi baru (std-only calendar);
  nol IO; diff terbatas ke `crates/n8n-common/src/{schedule.rs,lib.rs}` + dokumen.
- Tidak ada scheduler kedua: modul mengeluarkan `ScheduleExecution` (handoff) dan
  tidak pernah memanggil engine P3.
