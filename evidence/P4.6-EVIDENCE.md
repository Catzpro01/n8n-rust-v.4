# P4.6 Evidence — Event / Manual / Test / Waiting Ingress (Issue #108)

Slice: P4.6 · Modul: `crates/n8n-common/src/ingress_modes.rs` (plane + suite test).

## Hasil test (rig offline, exit 0)

- **262 tests green** workspace (252 P4.5 → +10 P4.6), 0 failures, 0 warnings.
- File slice lolos `cargo fmt` (rustfmt 1.8.0-stable).

## Coverage checklist #108

| Spesifikasi | Implementasi | Test |
|---|---|---|
| distinguish new vs resume | `IngressIntent::classify` (11 varian source) | `intent_classification_distinguishes_new_vs_resume` |
| preserve manual/listen first-emission | `manual_first_emission` + `ListenPool.consume` | `manual_trigger_preserves_first_emission`, `test_listen_fires_exactly_once` |
| test webhook no persistent prod route | bounded `TestListen` (retention + expiry) | `expired_listen_returns_none` |
| waiting resolve executionId | `resolve_waiting_path` (id+suffix) | `waiting_path_extracts_execution_id_and_suffix` |
| validate execution state | `waiting_verdict` (Ready/Running/Finished/Error) | `waiting_verdicts_map_like_n8n` |
| expired/not-found/error states | verdict → 404/409/401 | `resolve_waiting_emits_404_and_409` |
| duplicate resume protection | dedupe key = executionId | `resolve_waiting_emits_404_and_409` (`dedupe_key`) |
| correlation IDs | `ManualTrigger.correlation_id` | manual test |
| generation checks | watcher fence → `STALE_GENERATION` | `event_decision_checks_generation_fence` |
| bounded resource retention | pool ≤ 64 + expiry prune | `expired_listen_returns_none` (retention path) |
| verbatim event names | `EventName` bounded + `classify_event` | `event_names_and_actions_are_verbatim`, `event_name_is_bounded` |

## Gate-F (race/failure)

duplicate manual emission → 409; expired tests/triggers → none/410; double resume
(dedupe key); generation lawas watcher → STALE; event tak dikenal → 404 —
semua deterministik dan diuji.

## Batas

- Nol perubahan kontrak; nol dependensi; nol IO; diff terbatas
  `crates/n8n-common/src/{ingress_modes.rs,lib.rs}` + dokumen.
- Tidak menyentuh P3/P5/P6.
