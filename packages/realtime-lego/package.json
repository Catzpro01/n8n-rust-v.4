{
	"name": "@lego/realtime",
	"version": "0.1.0",
	"private": true,
	"description": "REALTIME LEGO — n8n 2.9.4 push subsystem (SSE + WebSocket backends, origin validation, pubsub relay). Rust NOT STARTED.",
	"license": "SEE LICENSE IN reference/n8n/LICENSE.md",
	"type": "module",
	"scripts": {
		"test": "node --test test/*.test.mjs",
		"gate": "node ../../tools/realtime-isolation-gate.mjs"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"typescript": "~5.9.2"
	}
}
