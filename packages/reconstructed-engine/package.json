{
  "name": "@n8n/reconstructed-engine",
  "version": "2.9.4",
  "private": true,
  "description": "Reconstructed n8n Workflow Execution Engine (Node.js/TS) — 1:1 from n8n v2.9.4, modular LEGO structure, zero Rust, frontend untouched",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --loader ts-node/esm test-run.mjs",
    "test:legacy": "node test-run.mjs",
    "verify": "node test-run.mjs"
  },
  "dependencies": {
    "luxon": "^3.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.9.2",
    "ts-node": "^10.9.0"
  }
}
