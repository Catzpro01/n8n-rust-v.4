{
  "name": "@lego/validation",
  "version": "0.1.0",
  "private": true,
  "description": "Validation LEGO (Phase 2) — type coercion, zod schemas, type guards, workflow structural validation (NEW CAPABILITY). Rust: NOT STARTED.",
  "license": "SEE LICENSE IN reference/n8n/LICENSE.md",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "zod": "^3.22.0",
    "luxon": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.9.2"
  }
}
