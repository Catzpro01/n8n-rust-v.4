{
  "catalogVersion": "1.0.0",
  "description": "The frontend's Context & Session surface: what it may render about a session, a context window, a rollover and a continuation, and what it must never become. Context & Session is ONE LEGO with TWO contracts (ai.context = what is loaded now, ai.agent-session = bounded state: identity plus references). The catalog ships EMPTY on purpose: this is a state surface, so it renders what it is handed and names every absence. Conversation, session, context window, memory and execution stay five distinct things; no generic AI-state object is derived from them.",
  "lego": "context-session",
  "contracts": [
    "ai.context",
    "ai.agent-session"
  ],
  "declarationSource": {
    "context": {
      "file": "apps/n8n-lego/src/lego/manifest/ai-foundation.json",
      "path": "context",
      "owner": "manager"
    },
    "session": {
      "file": "apps/n8n-lego/src/lego/manifest/ai-foundation.json",
      "path": "agentSession",
      "owner": "manager"
    },
    "lego": {
      "file": "apps/n8n-lego/src/lego/manifest/ai-lego-set.json",
      "path": "lego#id=context-session",
      "owner": "manager"
    },
    "capabilities": {
      "file": "apps/n8n-lego/src/lego/manifest/domains.json",
      "path": "domains#id=ai-foundation.capabilities[id=ai.context|ai.agent-session]",
      "owner": "manager"
    },
    "scenario": {
      "file": "apps/n8n-lego/src/lego/manifest/reference-scenarios.json",
      "path": "scenarios[id=context-rollover]",
      "owner": "manager"
    }
  },
  "publication": {
    "status": "declared-not-locked",
    "rows": [],
    "expected": [
      {
        "contract": "ai.context",
        "declaredVersion": "1.0.0",
        "owner": "manager",
        "domain": "ai-foundation",
        "lockedIn": "apps/n8n-lego/src/lego/contracts/contract-lock.json",
        "declaredIn": "apps/n8n-lego/src/lego/manifest/ai-lego-set.json#lego[id=context-session].versioning",
        "operations": [
          "ai.context.load",
          "ai.context.compact"
        ],
        "permissions": [
          "ai:context:read",
          "ai:context:write"
        ]
      },
      {
        "contract": "ai.agent-session",
        "declaredVersion": "1.0.0",
        "owner": "manager",
        "domain": "ai-foundation",
        "lockedIn": "apps/n8n-lego/src/lego/contracts/contract-lock.json",
        "declaredIn": "apps/n8n-lego/src/lego/manifest/ai-lego-set.json#lego[id=context-session].versioning",
        "operations": [
          "ai.agent-session.create",
          "ai.agent-session.status",
          "ai.agent-session.close"
        ],
        "permissions": [
          "ai:agent:create",
          "ai:agent:read",
          "ai:agent:control"
        ]
      }
    ],
    "notPublished": [
      "ai.context.rollover",
      "ai.context.rehydrate",
      "ai.context.verify",
      "ai.agent-session.continue",
      "ai.agent-session.pause",
      "ai.agent-session.resume",
      "ai.memory.*",
      "ai.agent-runtime.*"
    ],
    "note": "The state at the P2.13 baseline (main @ e754c5df), verified and not assumed: contract-lock.json publishes 15 rows and NO row for either contract, while domains.json registers both capabilities as contract-only with 5 operations and 5 permissions between them, and ai-lego-set.json CLAIMS ai.context@1.0.0 + ai.agent-session@1.0.0. A claim is reported as declaredVersion and never rendered as a published version. `rollover`, `rehydrate` and `verify` are declared LEGO verbs with no registered operation, so they are answered operation-unpublished; `pause`/`resume` belong to ai.agent-runtime, and no memory or runtime operation exists at any layer. When the manager locks the rows, `rows` carries them and the surface reports `published` without a code change."
  },
  "rendering": {
    "sessionIdentity": true,
    "sessionState": true,
    "contextScope": true,
    "contextLifecycle": true,
    "reportedUsage": true,
    "rolloverState": true,
    "continuationState": true,
    "verificationResult": true,
    "previousAndNextSession": true,
    "degradedContinuation": true,
    "pendingPublication": true,
    "tokenDashboard": false,
    "transcript": false,
    "fabricatedTokenCount": false,
    "memoryStore": false,
    "executionAffordance": false,
    "silentReset": false
  },
  "concepts": [
    "conversation",
    "session",
    "context-window",
    "memory",
    "execution"
  ],
  "rules": [
    "Conversation != Session != Context window != Memory != Execution. The five stay distinct in the UI, and a record that merges them is refused (assertDistinctConcepts).",
    "Every shared word is quoted from the backend declaration with provenance: 7 scopes, 9 context fields, 6 context lifecycle states, 14 continuation sections, 5 declared verbs, 2 registered context operations, 3 session operations, 7 session states, 10 session fields, 3 references, 5 permissions, 3 token kinds. Nothing is coined here.",
    "The rollover phase machine (NORMAL -> PREPARE -> ROLLOVER) and the verification results (verified / degraded / failed) are ruled but UNPUBLISHED, so they stay in PENDING_PUBLICATIONS with XA-20 and are rendered as pending; the published lifecycle words `prepare`, `compacting` and `rolled-over` are what the UI shows today.",
    "A usage figure is reported, estimated, not-reported or over-budget, and carries its token kind or is not rendered. No percentage is computed from nothing and no estimate is presented as a report.",
    "A rollover threshold must be declared and strictly below the bound: a threshold at or above 100% is refused, because a rollover at the limit has no room left to write the continuation package.",
    "Verification is explicit and has three results. Missing state is reported as missing and is never silently repaired; a degraded continuation is announced and a failed one is never rendered as a new session.",
    "State carries references, never payloads: a session holds contextRef, artifactRef and traceRef, and an inlined transcript, message list or context body is refused as unbounded.",
    "No credentials, tokens, secrets, cookies, authorization headers, chain-of-thought, raw or hidden prompts, transcripts, full model output, host paths or capability grants - refused by name and by key pattern.",
    "The published operations are exactly five (ai.context.load, ai.context.compact, ai.agent-session.create, ai.agent-session.status, ai.agent-session.close) and the UI offers none of them; `Continue session` is an intent answered operation-unpublished.",
    "No Memory store is implied: what may be counted is loaded context, decisions and artifacts, and the surface says that is what they are (XA-12).",
    "No execution affordance: the Agent Machine has no runtime, and this surface is not it.",
    "Records are supplied to the runtime by the application; this package never reads the backend tree, and nothing it returns enters the 18,126-byte boot descriptor.",
    "A declaration that moved past the quoted vocabulary is reported (declarationDrift) and never adopted silently; a version claimed in a file is reported as declaredVersion and is still not a publication."
  ],
  "contexts": [],
  "sessions": [],
  "alignment": {
    "decision": "XA-20",
    "status": "open-for-manager",
    "register": "docs/n8n-lego/decisions/cross-agent-decisions.json",
    "package": "docs/n8n-lego/decisions/XA-20-context-session-publication.md",
    "milestoneRegister": "docs/n8n-lego/milestones.json#milestones[id=P2.13]",
    "test": "packages/frontend-lego/test/32-context-session.test.mjs",
    "comparisonTest": "packages/frontend-lego/test/29-alignment.test.mjs",
    "rule": "A difference between this package's quoted vocabulary and the declaration it consumes is reported as data (declarationDrift) and registered, never resolved by adopting a word this package cannot quote. There is no tolerated difference: REGISTERED_DRIFT in test/29 is empty, so a backend that spells a scope, a state, a section or an operation differently fails the gate until the manager reconciles it.",
    "relatedDecisions": [
      "XA-12 (memory is not context; no store may be implied)",
      "XA-17 (which contract publishes token usage; the three token kinds are quoted with a pending record)",
      "XA-11 (where Skill is modelled - unaffected: P2.13 adds no domain)"
    ]
  }
}
