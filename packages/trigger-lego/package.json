{
  "name": "@lego/trigger",
  "version": "0.1.0",
  "private": true,
  "description": "Trigger LEGO (Trigger lifecycle, poller management, trigger registration) — Rust: NOT STARTED.",
  "license": "SEE LICENSE IN reference/n8n/LICENSE.md",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.9.2"
  }
}
