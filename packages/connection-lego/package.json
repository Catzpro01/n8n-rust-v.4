{
  "name": "@lego/connection",
  "version": "0.1.0",
  "private": true,
  "description": "Connection Model LEGO — Phase 3 Node.js/TypeScript reconstruction of n8n 2.9.4 connection routing (common/**, graph/graph-utils.ts, connections-diff.ts). Accepted by reference-derived golden cases.",
  "license": "SEE LICENSE IN ../../reference/n8n/LICENSE.md",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "reference:drift": "node ../../tests/reference/workflow-rust/build-fixtures.mjs --check"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.9.2"
  }
}
