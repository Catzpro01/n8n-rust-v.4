{
  "name": "@lego/connection",
  "version": "0.1.0",
  "private": true,
  "description": "Connection Model LEGO (Phase 3 - graph traversal & connection diff) — boundary, ports and pinned reference seam. Rust: NOT STARTED.",
  "license": "SEE LICENSE IN reference/n8n/LICENSE.md",
  "type": "module",
  "scripts": {
    "extract": "node ../../tools/connection-isolation-extract.mjs",
    "build": "npm run extract && tsc -p .extract/tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test test/*.test.mjs",
    "verify": "node ../../tools/connection-isolation-gate.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "n8n-workflow": "2.9.1",
    "typescript": "~5.9.2"
  }
}
