/**
 * P-KERNEL-TYPES — shared vocabulary consumed by the Connection LEGO (contract CD-07).
 * Types are compile-time only and come from the pinned reference; nothing here is owned by this LEGO.
 */
export type {
	IConnection,
	IConnections,
	INodeConnection,
	INodeConnections,
	NodeInputConnections,
	NodeConnectionType,
} from 'n8n-workflow';
