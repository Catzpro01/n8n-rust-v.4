{
  "_source": "contracts/validation.contract.md §3/§4.4 (ISSUE-003 Option A, arbitrated): CycleDetection over main edges reports the first back-edge deterministically. Suite-negative fixture: per the -invalid directory rule the cycle MUST be detected — a validator hardcoded to accept everything turns the gate red.",
  "negative": true,
  "cases": {
    "cycle-detected": {
      "probe": { "op": "detectCycles" },
      "expected": {
        "code": "CYCLE_DETECTED",
        "node": "A",
        "message_prefix": "Cycle detected: "
      }
    },
    "validate-workflow-strict": {
      "probe": { "op": "validateWorkflow", "allowCycles": false },
      "expected": { "valid": false, "codes": ["CYCLE_DETECTED"] }
    },
    "validate-workflow-default-allows-cycles": {
      "probe": { "op": "validateWorkflow", "allowCycles": true },
      "expected": { "valid": true, "codes": [] },
      "explains": "contract §11.7: default allowCycles: true for reference parity (n8n allows runtime loops, e.g. Loop Over Items)"
    }
  }
}
