# 05-empty-data

- `Empty` -> `main: [[]]`; `After` absent from runData (never executed); status `success`.
- `EmptyAlways` (`alwaysOutputData: true`) -> `main: [[{json:{}, pairedItem:[{item:0,input:0},{item:1,input:0}]}]]`; `AfterAlways` executes.
