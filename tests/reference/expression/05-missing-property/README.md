# 05-missing-property

Missing property access is lenient (undefined / '' in strings) while missing NODES and missing EXECUTION DATA are hard `ExpressionError`s. See expected.json for the exact error `type`/`descriptionKey` values.
