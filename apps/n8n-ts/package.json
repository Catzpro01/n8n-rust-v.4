{
  "name": "@n8n-ts/baseline-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript LEGO baseline runtime — node:http server + reconstructed-engine (zero runtime deps).",
  "license": "SEE LICENSE IN ../../reference/n8n/LICENSE.md",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --experimental-strip-types src/server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.9.2"
  }
}
