{
  "name": "n8n-rust-v4",
  "version": "0.4.0",
  "private": true,
  "description": "n8n 2.9.4 anatomy → contract → reference runtime → LEGO isolation → Phase-3 reconstruction (JS/TS track + Rust port confined to crates/**, apps/**)",
  "scripts": {
    "setup:reference": "bash scripts/setup-reference-runtime.sh",
    "verify": "node tools/workflow-isolation-gate.mjs",
    "verify:fast": "node tools/workflow-isolation-gate.mjs --skip-live",
    "isolation:map": "node tools/workflow-boundary-map.mjs --json packages/workflow-lego/manifest/boundary-map.json --md docs/isolation/workflow-dependency-map.md",
    "isolation:check": "node tools/workflow-boundary-map.mjs --check && node tools/workflow-kernel-conformance.mjs --check && node tools/workflow-port-surface.mjs --check && node tools/workflow-reference-manifest.mjs --check",
    "isolation:extract": "node tools/workflow-isolation-extract.mjs",
    "workflow-lego:build": "npm --prefix packages/workflow-lego run build",
    "workflow-lego:test": "bash scripts/run-lego-tests.sh",
    "reconstructed-engine:test": "node --test packages/reconstructed-engine/*.test.mjs",
    "execution:test": "node --test packages/execution-engine/test/*.test.mjs",
    "execution:gate": "node tools/execution-engine-gate.mjs",
    "connection-lego:test": "npm --prefix packages/connection-lego test",
    "trigger:test": "npm --prefix packages/trigger-lego test",
    "trigger:gate": "node tools/trigger-lego-gate.mjs",
    "webhook:test": "npm --prefix packages/webhook-lego test",
    "webhook:gate": "node tools/webhook-lego-gate.mjs",
    "scheduler:test": "npm --prefix packages/scheduler-lego test",
    "scheduler:gate": "node tools/scheduler-lego-gate.mjs",
    "reference:fixtures": "node tests/reference/workflow-rust/build-fixtures.mjs --check",
    "workflow-model-lego:test": "npm --prefix packages/workflow-model-lego test",
    "verify:all": "npm run isolation:check && npm run reconstructed-engine:test && npm run execution:gate && npm run connection-lego:test && npm run workflow-model-lego:test && npm run trigger:gate && npm run webhook:gate && npm run scheduler:gate"  }
}
